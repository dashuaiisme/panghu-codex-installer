# 胖虎AI客户端 · 全量逐文件审查报告（完整覆盖）

> 生成：2026-07-08 · 6 个并行 agent **逐文件通读**全部源码/测试/脚本/文档后整合
> 范围：src/ 全部 9 文件（含 9077 行主文件与 4204 行 index.html）、tests/ 16 文件、scripts/ + 根 _* 脚本共 30 文件、根 12 文档、docs/ 9 文档
> 本文 **取代** 早先的《优化审视报告_2026-07-08.md》（那一轮是分维度抽样，非逐文件）

---

## 〇、覆盖声明（这次真的全读了）
| 分组 | 文件数 | 读取方式 |
|---|---|---|
| 主程序 `panghu_ai_client.py` | 1（9077 行/412KB） | 逐行通读 |
| 其余 src + `ui/index.html` | 8 | 逐文件通读 |
| tests | 16 | 逐文件通读 |
| scripts + 根 `_*` 脚本 + CI | 30 | 逐文件通读 |
| 根文档 | 12 | 逐文件通读 |
| docs/ 子树 | 9 | 逐文件通读 |

---

## 一、跨文件的 P0 级硬伤（必须修）

### 1. 发布闸门永不 FAIL（安全/质量闸门形同虚设）
- `scripts/commercial_release_acceptance.py:572`：`return {"status": "PASS" if not warnings else "WARN"}` —— **顶层只有 PASS / WARN，永不 FAIL**。
- 包内 self-test 失败只是被加进 warnings：`scripts/commercial_release_acceptance.py:568-569`。
- CI 仅拦截非 PASS：`build-mac-release.yml:54-56,199-205` → **WARN（含 self-test 失败/缺包）照样上传**。
- `scripts/commercial_flow_acceptance.py:862` 把 `status` **硬编码为 PASS**，同时却列出 5 条 `blocking_gaps` 被忽略。
- 唯一会 FAIL 的是 `agent_delivery_acceptance.py:285`。
- **修法**：self-test==FAIL 或产物缺失/陈旧 → 顶层 FAIL；CI 把 WARN 也当拦截。

### 2. "交付成功"在校验层被放水（违反商业合同）
- `src/commercial_core.py:1108-1133` `verify_client_scope_delivery_evidence`：仅当 `client_installed and config_written` 就 `passed=True`（→ 扣费），**并未做真实对话验证**，与商业合同"无 real_task_verified 不得判定交付成功"直接冲突。
- 注：主文件 `panghu_ai_client.py:8225/8459` 的部署主流程确有 `verify_real_task_evidence` 闸口，但 `commercial_core` 这一层是假的。两层口径不一致，需统一为"必须真实验证"。

### 3. 前端预览桩硬编码业务数据（违反 AGENTS.md 硬性条款）
- `src/ui/index.html:1939-1944`：预览桩硬编码 `price_cents:9900/29900/19900`、标题"GPT Plus 代充"、`amount_cents:9900*qty`、`card_codes:["PHK-PREVIEW-0001"...]`、`plus_tool_url`。AGENTS.md 禁止硬编码价格/卡密/真实交付成功，且这些随成品发布。
- `src/ui/index.html:2147`：`get_initial_state()` 失败 `.catch(() => state = buildPreviewState())` —— **真实后端挂掉会静默掉进假预览模式显示桩数据**，危险。

### 4. 文档真相彻底打架（FINAL_REPORT 与其他 11 文档方向相反）
- `FINAL_REPORT.md:5-13`（07-07 基线，标注"以此为准"）称"连接通讯软件已上线生产""客户端 367 / 服务端 164 全绿"。
- 但 `TESTING/INTEGRATION/HANDOFF/ARCHITECTURE/PRODUCT/ACCEPTANCE/RUNBOOK/SECURITY` 共 11 个文档停留在 **07-03 的"WARN/未完成/不发布"口径**，且 `INTEGRATION.md:139-143` 与 `HANDOFF.md:100` 把 `communication_software_link/agent_center/gpt_plus` 标为 `pending_production`，与 FINAL 基线直接矛盾。
- **两方必有一错**，需先向用户核实真实状态再统一口径，不能二者并存。

### 5. 多重并行"活跃交接"违反治理规则
- `AGENTS.md:33` 宣称"仅一份最新交接 = `交接说明_2026-07-07_Claude.md`"。
- 实际 `docs/` 并存 `回来后收尾步骤_2026-07-08.md`、`汇总_AI订阅与Plus充值工具整链_2026-07-08.md`，且 07-08 比声明"最新"更新；`交接说明_2026-07-07` 自身 §0.4/§0.5 还并排了 Fable5/Cowork 与 Claude 两个会话的不同"当前真相"。
- `CHANGELOG.md:57,85` 仍引用已清理的旧带日期交接（`CLI交接文档_2026-07-03.md` 等）。

---

## 二、主文件 `panghu_ai_client.py`（逐行发现）

| 类别 | 位置 | 问题 |
|---|---|---|
| 死代码 | `:4624-4632` | `detect_environment()` 先算 `system/lines` 又被立即覆盖，前 9 行是死代码，删 |
| 重复 | `:4689-4723 / 5633-5667 / 6049-` | `agent_matrix_state` 逻辑复制 3 份，抽 `_build_agent_matrix_states()` |
| 重复 | `:4736-4764 / 5672-5704` | `get_initial_state` 与 `sync_webview_state` 的 state dict 近重复 ~30 项，建一次复用 |
| 死参数 | `:1282-1298` | `build_config(api_key, base_url, model)` **忽略 base_url 和 model**，永远写死 `base_url` 和 `model="gpt-5.5"`；调用方传参无效 |
| 写死 | `:1142-1150 / 1283-1298` | `model="gpt-5.5"`、`model_context_window=1000000` 等写死 |
| 写死 | `:1295 / 171 / 8918` | `"https://aitokenapi.cc/v1"` 多处硬写，与 `DEFAULT_BASE_URL`（`:160`）不一致，常量一改就过期 |
| 写死 | `:3005 / 3013 / 2994` | GitHub fallback URL `https://github.com/dashuaiisme/panghu-ai-client/releases` 硬写 |
| 吞异常 | `:7633,264,273,348,367,398,408,976,1018,1029,1036,541,2769,2687` | 大量 `except: pass`（含 DPAPI 失败静默降级、cookie jar、chmod、DPI），无日志 |
| 半成品 | `:7927-7932` | webview 自动更新"打包发布当前叫停中…后续接前端确认对话框"未完 |
| 半成品 | `:2828-2831 / 2878-2880` | `embedded_webview_blocked` 原因文案"胖虎AI网站内置浏览器闭环未完成"——残留 |
| 脆弱路由 | `:2870` | `embedded_customer_page_title` 用 `"payment" in safe`/`"/buy" in safe` 子串判定内嵌，**不校验 scheme/域名**（前轮已报 P0，此处确认行号） |
| 脆弱 | `:4083-4090` | drift 检查比对精确环境变量字符串，拼写一变就误报"红" |
| 超长函数 | `:1835-2125`（~290 行 dispatch）、`:8331-8663`（_deploy_worker ~330 行）、`:8168-8330`、`6675-6804`、`4572-4614` | 应拆为 dispatch dict / 按 agent 拆分 |
| 导入 | `:23` | `from enum import Enum` 疑似未用，核实后删 |
| 安全 | `:2790-2791` | `build_webview_cookie_bridge_script` 注入 `document.cookie`；值已按 aitokenapi.cc 过滤并 json 转义，低风险但需确认 cookie 值不破坏 JS 字符串上下文 |
| 安全 | `:340-350` | 非 Windows 回退 `local-v1` XOR（密钥=可逆向 device_fingerprint），弱混淆（前轮已报 P1） |
| 合规 | `get_initial_state` | 返回空的 `loginPassword/apiKeyValue`，无密钥泄漏；部署成功闸口需 `verify_real_task_evidence` —— **主文件本身无硬编码价格/无伪造交付**，符合 AGENTS 规则 |

---

## 三、其余 src + UI（逐文件发现）

**commercial_api.py**
- `:904` `price_key = "price_" + "cents"` —— 字符串拼接规避字面量扫描，混淆味，改回 `"price_cents"`。
- `:366-378` `sanitize_commercial_api_url` 依赖 `SENSITIVE_TEXT_PATTERNS[-1]` 元组**末尾顺序**做脱敏，顺序一改静默失效。
- `:414` `except Exception: detail = ""` 吞掉服务端错误体解析。
- 重复：幂等键 + `CommercialApiRequest` 构造重复 ~30×；`_require_current_buyer_operator` 在 `commercial_backend_contract.py:288` 又复制一份。
- 死契约面：多数 `build_*_request`（agent/admin/ledger）在本仓无调用方（仅服务端用）。

**commercial_core.py**
- `:1108-1133`（见 P0#2）`verify_client_scope_delivery_evidence` 假验证。
- `:904` 同上 `price_cents` 拼接 hack。
- 重复：`AGENT_CENTER_REQUIRED_SUMMARY_FIELDS`（`:52-60`）+ 摘要构造逻辑在 `commercial_api.py / commercial_core.py / commercial_backend_contract.py:277-285` **三处重复**。

**commercial_backend_contract.py**
- `:11-12,124-125` 类注释警告"不得当本地状态机"，但 `CommercialLedgerContract` 实为**完整本地代理/佣金/结算引擎** —— 自相矛盾，且属 AGENTS.md 禁止的"本地硬编码业务规则"。其自身 docstring 说应删除。
- `:540` `invite_code = f"invite-{user_id}".replace("_","-")` —— 由 user_id 派生，可猜测/碰撞。
- 全程 `"day-0"/"day-N"` 假时间戳（`:547,585,629,1163,1329`）—— mock/stub 标记。
- `:539` `status="pending_review" if ... or product.price_cents > 0` —— 任何付费 agent 强制审，意图不明。

**commercial_manifest_public_key.py**：仅硬编码 Ed25519 公钥，正常，无问题。

**panghu_agents.py**：`:11` `from panghu_constants import *` 通配导入味；`CodexConfigMode` 多数枚举值弱使用（轻微死面）。

**panghu_constants.py**
- 写死 URL：`aitokenapi.cc / sim.aitokenapi.cc / plus.aitokenapi.cc`、GitHub release（集中化可接受但注意）。
- **前后端不一致**：`MODULE_VALUE_ADDED` 子栏 key = `gpt_plus/phone_card/sms_code`（`:63-67,160-164`），但 `index.html:2064` 用 `ai_subscription/sms_code` —— **键不匹配**。
- `AGENT_CENTER_URL = /agent`（`:55`）vs 前端 `pageMeta /agent-center`（`:2080`）、`joinUrl /agent/join`（`:3283`）—— 后端深链可能错。

**index.html（前端，4204 行）**
- 契约守住：`#loginGate:1687`、`#consoleLayout:1768`、`updatePythonState:2268`、`appendPythonLog:4162`、`renderLogs:4194`、4 模块 `:2041-2046` ✓；XSS 防护较好（`escapeHtml/jsStringInAttr/safeHttpUrl:3334-3336,3085-3097`）。
- 预览桩硬编码（见 P0#3）：`:1939-1944`。
- `:2147` 初始化失败静默掉进假预览（见 P0#3）。
- `:3069` 订阅加载 `.catch(() => state.subscriptionProducts = [])` 静默隐藏后端失败。
- 缺失桩方法 `build_register_url`（被 `:3843` 调用）在 `isBrowser` 桩里**不存在** → 浏览器预览抛错；大量 `communication_software_link_*` 在桩里是 no-op，桥脆弱。
- `:2432` `state.agentEnabled[...]` 读取但 `state`（`:1884`）未初始化 —— 靠兜底，近似死字段。
- `:3013/3029` `try{}catch(e){}` 吞选择区间错误（无害但吞了）。
- 预登录 `logsData` `:1919-1924` 硬编码示例时间戳，握手前就显示。

---

## 四、测试（16 文件逐读）

- **结论**：全部 16 个文件都是 `unittest.TestCase` 且带真实断言，**无纯 print/空 stub**。`commercial_*` 模拟器覆盖极好；**412KB 主文件的核心部署编排几乎零覆盖**。
- **脆弱 grep 型断言**：`test_commercial_backend_contract_docs.py`（仅 `assertIn(term, text)` 查文档关键词）、`test_commercial_build_scripts.py`（查构建脚本字符串）、`test_panghu_commercial_manifest.py:108` 直接 `assertIn('parse_payment_status_data(...)', source)` 断言源码文本、`test_commercial_core.py:468`/`test_panghu_commercial_manifest.py:769` 用 `assertNotIn("class X", source)` 做"删除回归"——文档/源码一改即红，应改 import 断言。
- **覆盖盲区**：`InstallerApp` 部署 worker（`do_install/do_config/do_launch/do_real_task`、`run_real_task_probe`、正常部署探测）未单测；GUI 构建/线程调度未测；`execute_commercial_api_with_trusted_certs` 真实网络路径仅 mock。
- **重复 fixtures**：`FakeVar` 类复制 **14+ 次**（`test_installer_backend.py:307,348,406,529,570,603,680,737,801,874,939,998,1039,1124` 等）；`FakeResponse` 多文件复制；ed25519 密钥对+签名逻辑重复 3 处；env 保存/恢复、`run_script` 辅助散落多文件 → 应抽公共 `conftest`/helper。
- **高危**：`test_panghu_commercial_manifest.py:255,271` 在**仓库根**直接建 `tmp-test-web-profiles`/`tmp-test-agent-assist-cleanup`，仅靠 finally `rmtree`，异常先于 rmtree 则留垃圾 → 改 `TemporaryDirectory`。
- 无 `@unittest.skip`、无死测试；硬写测试密钥 `sk-test-secret-123456` 合理（非真实）。

---

## 五、脚本（30 文件逐读）

- **应删/合并的根 `_*` 脚本（多为"跑完可删"残留）**：
  - `_build_all.bat`+`_build_all.ps1`（仅重跑 scripts/ 下三个 acceptance + build，`.ps1:1` 自标"跑完可删"）→ 删，直接调 scripts/。
  - `_postbuild_verify.bat`+`.ps1`（重复 `commercial_release_acceptance.py --with-exe-self-test`，CI 已调）→ 删。
  - `_do_commit.bat`+`.py`（提交助手，写死消息/日期，"跑完可删"）→ 删。
  - `_finalize_client.bat`（与 `_run_client_tests.bat`/`_do_commit.py` 重叠）→ 删或拆。
  - `_run_tests_20260707.bat`（写死 `胖虎AI后台管理系统` 与 `C:\Users\Administrator...`，"跑完可删"）→ 删，留 `_run_client_tests.bat`。
  - `_preview_server.py`+`预览界面.bat`（仅 UI 预览，不应随包）→ 不发布。
  - `_idx.js`（finalize/run_tests 生成的临时产物）→ 删或 gitignore。
  - 保留：`verify-windows.bat`（真实本地校验，但 `:48` "Cowork 沙箱"注释过时，删）、`scripts/run-*.bat`、`build-*.bat/.ps1/.command`、各 acceptance/test 脚本、`generate-download-qr.py`。
- **写死绝对路径**：`_build_all.ps1:3`、`_postbuild_verify.ps1:3`、`_run_tests_20260707.bat:3,4,7` 写死 `C:\Users\Administrator\Documents\codex\胖虎AI客户端` → 改用 `$PSScriptRoot` / `%~dp0`。
- **写死公钥**：`_build_all.ps1:19-23` 把**生产 Ed25519 公钥 PEM 内联**进 `PANGHU_COMMERCIAL_MANIFEST_PUBLIC_KEY_PEM`（公钥虽非密，但破坏轮换、与 CI secret 设计冲突）→ 移入 CI secret，仓库不内联。
- **静默失败**：`_build_all.ps1:2`/`_postbuild_verify.ps1:2` `$ErrorActionPreference="Continue"` → 改 `"Stop"` 并传播退出码；`_build_all.bat:5` 永远写 `_build_done.txt`；`build-mac-app.command:93` `codesign ... || true` 吞失败。
- **发布闸门**（见 P0#1）：`commercial_release_acceptance.py:572` 永不 FAIL；`commercial_flow_acceptance.py:862` 硬写 PASS 忽略 `blocking_gaps`。

---

## 六、文档（根 12 + docs/ 9 逐读）

**测试数字漂移全景**（同一事实多处不同值，权威应归 TESTING.md 却停在 338）：
`338`(TESTING:45,96 / FINAL §4) · `359`(FINAL 0.0/0.-1 / CHANGELOG:31,39) · `367`(FINAL 基线/交接客户端) · `370`(交接 §1) · `371`(交接 §7) · `198`(汇总服务端) · `174`(交接 §0.4 服务端) · `164`(FINAL/交接 §0.5 服务端) · `163`(CHANGELOG:11) · `370/371`(CHANGELOG:21,11)。**服务端计数 164/174/198 三处不一致**；同一 `交接说明` 内 §0.4/§0.5 服务端 174 vs 164 自相矛盾。

**逐文件要点**
- `FINAL_REPORT.md`：顶部 07-07 基线（称"以此为准"）与正文"仍非收口/连接通讯软件未完成/公钥未注入"**同文件自相矛盾**；`line 47` 自述"不重复维护数字"却在 `line 11` 列 367/164，违反 AGENTS.md:56"数字只在 TESTING.md"。
- `AGENTS.md:70` "分层拆分以降体量" 与 9077 行现实矛盾；`:33` "仅一份最新交接" 已被破（见 P0#5）。
- `README.md:81-88` 目录漏列已拆分的 `panghu_constants/agents/paths.py`（AGENTS.md:72-74 说有）。
- `INTEGRATION.md:139-143`（接入卡总表 `pending_production`）与 `FINAL` 基线（`line 9` 已上线）冲突；`:161` JSON 示例把 `gpt_plus` 写成 `available`，与总表自相矛盾。
- `PRODUCT_MANUAL...md:L61/L372` 称 Gemini"已接入完整配置链路"，但 `FINAL_REPORT:L83/137` 与 `发送客户说明.txt:L45` 均称"待接入/不写 Key" —— **产品手册夸大**。
- `docs/COMMERCIAL_BACKEND_API_CONTRACT.md`：合规良好，是权威合同，与根手册一致（客户端不得硬编码价格/返佣；真实交付须 `real_task_verified`）。
- `docs/superpowers/plans/2026-06-19-...md`：已自标非权威、含硬编码价（双态 20 元、商业包 30/88/188 元）但已免责；属 06-19 游离旧稿，应清。
- `docs/方案_增值业务重构与代理中心_2026-07-07.md:L3` 标"方案讨论稿，未开始改代码"，但 `汇总_2026-07-08`/`回来后收尾步骤` 称已落地 → 状态陈旧；且其"删除手机卡/连接通讯软件"若已落地，产品手册未同步。
- `docs/发送客户说明.txt`：基本合规（代理/返佣以服务端为准），仅 Gemini 状态与产品手册冲突（同前）。

**合规确认**：所有 docs（合同/手册/方案/客户说明）均**正确声明**客户端不得硬编码价格/返佣/次数/真实交付成功，无文档把 stub 包装成 done 的明确违规。

---

## 七、建议推进顺序（全量版）

1. **P0**：发布闸门加 FAIL 档（self-test 失败/产物缺失/含私钥即硬失败）；`commercial_flow_acceptance` 状态由 blocking_gaps 决定。
2. **P0**：修 `commercial_core.py:1108-1133` 假交付验证，统一到"必须 real_task_verified"。
3. **P0**：删/标注 `index.html:1939-1944` 预览桩硬编码价格卡密；`get_initial_state` 失败给可见报错而非掉进假预览（`:2147`）。
4. **P0**：与用户核实真实状态（连接通讯软件/WAP 是否真在生产、公钥是否真注入），统一 FINAL 基线与其余 11 文档，并清理多余带日期交接。
5. **P1**：主文件抽离——`build_config` 死参数与写死 `gpt-5.5`/URL、device_fingerprint+DPAPI 原语、商业 dispatch、GUI/WebviewApi；合并 3 处 `agent_matrix_state` 与 2 处 state dict；补吞掉的异常日志。
6. **P1**：删根 `_*` 残留脚本、`_idx.js`、内联公钥；路径改相对；`$ErrorActionPreference` 改 Stop。
7. **P1**：`commercial_backend_contract.py` 的 `CommercialLedgerContract` 本地账本引擎按自身 docstring 删除；`invite_code` 改不可猜测；清 "day-N" 假时间戳。
8. **P1**：前后端键对齐——`MODULE_VALUE_ADDED` 子栏 key、`AGENT_CENTER_URL` vs `/agent-center`。
9. **P2**：测试抽公共 fixtures（`FakeVar/FakeResponse/签名`）；仓库根临时目录改 `TemporaryDirectory`；给 412KB 主文件部署 worker 补单测；grep 型删除回归改 import 断言；`panghu_constants` 写死 URL 集中化；补 `logging` 替换 print。

---

## 八、一句话结论
这一轮把**每一个文件都读透了**，确认早先抽样版的结论成立且更严重：除了之前发现的 URL 内嵌绕过、主文件未瘦身、发布不 FAIL、文档打架之外，还揪出 `commercial_core` 里**交付验证被静默放水**、`commercial_backend_contract` 里**藏着违规的本地账本引擎**、前端**预览桩硬编码价格卡密**、以及**测试数字在 8+ 个文档间互不相同**等硬伤。项目"半成品"的定性非常扎实——骨架和契约在，但质量闸门、文档真相、部分业务逻辑都处于"看起来像完成、实际是桩/放水"的状态。
