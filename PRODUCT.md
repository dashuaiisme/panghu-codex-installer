# 产品说明与项目蓝图

最后更新：2026-07-03

本文件是仓库入口级产品摘要与项目蓝图（原 `PROJECT_BLUEPRINT.md` 已并入本文件）。完整产品规则以 `docs/PRODUCT_MANUAL_SINGLE_SOURCE_OF_TRUTH.md` 为准。

## 1. 产品定位

“胖虎AI客户端”是面向买家和新手用户的一站式 AI 客户端服务工具。客户先登录胖虎AI账号，再在同一个桌面工具内进入 Agent 配置、胖虎AI网站、增值业务和代理中心等流程。

它不是单纯的脚本工具，也不是胖虎AI网站控制台、支付、钱包或后台管理系统的替代品。

产品主从口径：

- 胖虎AI客户端是主产品、客户统一入口和顶部业务容器。
- 胖虎AI中转站、手机接码、Plus 充值 / Plus 订阅、连接通讯软件、代理中心都属于客户端里的功能区或分支服务。
- 胖虎AI中转站只按 API 网关、API Token、余额扣费、模型调用、用量记录、模型价格、网关侧充值记账和必要 token 返佣分支承接，不承担整个平台后台或客户端后台定位。
- 独立胖虎AI后台管理系统负责账号、订单、支付、权益、代理、服务目录、运营配置和跨服务编排。

## 2. 核心模块

- 配置Agent：处理 API Key、环境检测、Agent 安装配置、启动检测、最小中文对话和功能验收矩阵；连接通讯软件的配置和验收入口固定在本模块。
- 胖虎AI网站：通过内置浏览器打开服务端页面，例如控制台、充值购买、创建 Key 和推广返佣。
- 增值业务：展示服务端下发的 Plus 订阅、账号服务、手机卡/云号码、手机号/短信接码、连接通讯软件等入口。
- 代理中心：承接本工具独立代理业务入口，数据、等级、返佣和结算均以服务端快照为准。

## 3. 产品目标

- 客户登录胖虎AI账号后，尽量在同一个工具内完成账号、API、中转站、Agent、充值购买、会员服务、手机号/短信接码、连接通讯软件和交付验收流程。
- 查看账号与权益状态。
- 打开胖虎AI网站的注册、邀请码、充值购买、创建 API Key、代理中心等服务端页面。
- 完成 Codex、ClaudeCode（CC）、OpenClaw、Hermes、Gemini / agy（Google Antigravity）的安装、配置、启动检测、最小中文对话验收和交付矩阵确认。
- 作为独立增值服务配置“连接通讯软件”，把已可用 Agent 接入通讯软件或办公协同通道；该服务独立下单、独立配置、独立验收、独立收费，不回滚基础 Agent 配置交付。
- 通过“增值业务”承接 GPT 会员代充、账号服务、手机号/短信接码等服务端下发入口；价格、库存、履约、有效期和上架状态都以服务端为准。
- 生成客服可读的 Execution Trace Log 和最终交付结论。

完成态必须以真实客户闭环为准，不能以单元测试通过、局部截图或假数据演示替代。

## 4. 范围

当前范围内：

- WebView 正式客户主界面：登录闸口和登录后主控制台均以 `src/ui/index.html` 为准。
- 顶部一级模块：配置Agent、胖虎AI网站、增值业务、代理中心。
- 内置网站入口与 WebView 会话桥接机制；不允许回退旧 Tkinter 业务界面或系统浏览器冒充闭环。
- 代理中心模块的服务端合同框架：token 返佣、下游付费激活返佣、付费安装 Agent 返佣、下游客户和工具代理后端。
- 已接入 Agent 的安装、配置写入、启动检测、最小对话验收框架；Gemini / agy 走胖虎AI网关 Gemini 格式配置（写入 `~/.gemini/.env`）。
- 连接通讯软件的产品、后端合同、入口、验收和安全边界；支持已有 Agent 检测或人工复核，不把入口硬绑定到本次基础 Agent 配置完成。
- 商业合同、manifest 验签、权益、配置会话、扣次与交付矩阵代码框架。
- 工程文档、测试、截图和验收流程。

当前范围外：

- 生产服务器、数据库、支付、钱包、下载页、`latest.json`。
- GitHub Release、客户 zip/exe/app 重包与发布。
- 维护第二套 Tkinter 业务界面。
- 用本机残留配置冒充真实客户交付。

## 5. 产品能力结构

登录前：

- 只显示登录闸口。
- 只显示最少说明、账号输入、注册入口和公共域名。
- 不显示完整控制台、Agent 矩阵、权益详情或执行日志。

登录后：

- 必须覆盖品牌账号、业务入口、任务流程、主操作区、权益 / Agent 交付状态、日志或诊断能力。
- 具体布局、导航、面板、抽屉、命令区、状态区和日志区由 Gemini / agy 自由决定，不再固定为 TopBar / 顶部导航 / 左侧 / 右侧 / 底部日志结构。

登录页支持账号提示、历史账号下拉、可选记住密码和自动登录；记住密码只使用本机系统加密，退出或删除账号时会清理对应买家会话。

## 6. Codex 三种配置模式

- 普通模式：默认模式，Codex 使用胖虎AI中转站 API，消耗胖虎AI额度，不需要登录 ChatGPT 账号。
- 双态模式：保留客户自己的 ChatGPT 登录态，但模型请求仍走胖虎AI中转站 API，消耗胖虎AI额度。
- 官方直登：Codex 使用客户自己的 ChatGPT 账号登录态，消耗 ChatGPT 账号额度，不写入胖虎AI中转站 Key。

胖虎AI账号只用于登录本工具和胖虎AI网站，不是 Codex 登录账号。所有 Codex 配置模式写入后，都必须完全退出 Codex 再重新打开才会生效。切换前保存本机配置快照，目标目录为 `~/.codex/panghu_modes/`。

## 7. 固定边界

- 胖虎AI公共域名固定为 `https://aitokenapi.cc`。
- 注册、邀请码、创建 Key、充值购买、代理中心必须走胖虎AI网站或服务端。
- 代理身份只作为登录后账号权益展示。
- 代理中心不能等同于网站推广返佣页；它必须预留 token 返佣、下游付费激活返佣、付费安装 Agent 返佣和工具代理后端合同。
- API Key 不输出到日志。
- 不保存可恢复登录 token 或部署授权 token。
- `profile.json` 只保留账号提示、API Key、模型和界面偏好。
- 未通过功能验收矩阵不得包装成完整交付。
- 连接通讯软件不得作为基础 Agent 配置的子状态；不得共用扣费事件；已形成连接通讯软件验收证据后，客户断网、禁 Key 或取消平台授权不得自动造成免单。

## 8. 当前产品边界

- Codex、ClaudeCode、OpenClaw、Hermes 是当前已接入或正在验收的主要 Agent 链路。
- Gemini / agy（Google Antigravity）配置链路已实现（胖虎AI网关 Gemini 格式）；与其他 Agent 一样，未通过最小中文对话验收前不计完整交付。
- 连接通讯软件是独立增值服务，不能和基础 Agent 配置共用订单、配置会话、验收记录或扣费事件。
- 手机号接码控制中心和 Plus session.脚本工具是胖虎AI客户端的关联增值服务项目；客户端只承接入口、登录会话和服务端状态摘要，不保存短信内容、号码分配规则、Plus Session Token、激活服务密钥或履约密钥。
- 未完成真实网页登录、充值购买、创建 Key、Agent 最小中文对话、代理中心服务端合同和客户功能验收矩阵前，不能声明最终客户交付完成。

## 9. 工程边界

- 产品定义只从 `docs/PRODUCT_MANUAL_SINGLE_SOURCE_OF_TRUTH.md` 取。
- 技术实现和构建规则只从 `docs/TECHNICAL_MAINTENANCE_MANUAL.md` 取。
- 运行命令只从 `RUNBOOK.md` 取。
- 当前真实进度和阻塞只从 `FINAL_REPORT.md` 取。

不允许把 README、分支交接包、过期截图、聊天上下文当成新的长期规则源。

## 10. 分工边界

前端审计或实现：

- 负责布局、信息层级、视觉稳定性、登录前后分层、截图验收。
- 不负责商业合同、扣次、服务端规则。

后端审计或实现：

- 负责登录、授权、manifest、权益、配置会话、扣次、日志脱敏、`profile.json` 边界。
- 不负责视觉美化。

主线：

- 负责统一文档、集成结果、测试、验收、最终状态报告。
- 不应在上下文已经过长时继续无边界并行大改前后端。

## 11. 完成态原则

节点只有满足以下条件，才可标为已验证：

- 有代码实现或明确外部服务入口。
- 有自动测试、手动操作证据、截图、日志或矩阵记录。
- 没有违反产品手册和技术维护手册。

单元测试通过只代表代码层验证通过，不代表客户可交付。

## 12. 禁止事项

- 不在本地硬编码价格、次数、有效期、设备数、返佣比例、返佣金额、代理等级、下游客户数、结算金额或商品上架状态。
- 不用离线 mock、单元测试、截图或本机残留配置冒充真实客户闭环。
- 不把未接入完整链路的 Agent 包装成完整付费交付。
