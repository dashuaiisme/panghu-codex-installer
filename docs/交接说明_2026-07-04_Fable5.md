# 交接说明 → 新会话（Fable 5）

日期：2026-07-04
交接人：Claude Code CLI（Opus 4.8 会话）
接手人：Claude Code CLI（Fable 5 新会话）

---

## 0. 一句话现状

**两个项目代码全部就绪、测试全绿、完整商业链路已在本地联调栈真机验证通过；唯一待办是"生产部署"（把后台管理系统搬上云服务器），此事需用户明确批准后按 hub 的 PRODUCTION_LOCK 流程执行。**

- 胖虎AI客户端：**359 tests + 187 subtests 全绿**
- 胖虎AI后台管理系统：**77 tests 全绿**
- 打包发布：用户叫停中，勿动

## 1. 项目位置

| 项目 | 路径 | 说明 |
|---|---|---|
| 客户端 | `C:\Users\Administrator\Documents\codex\胖虎AI客户端` | 买家桌面客户端（Python + pywebview），主程序 `src/panghu_ai_client.py`（~1.1万行），UI `src/ui/index.html` 单文件 |
| 后台管理系统 | `C:\Users\Administrator\Documents\codex\胖虎AI后台管理系统` | FastAPI 服务端（代理返佣/提现/管理后台/通讯软件/支付宝），**已对齐客户端合同，未部署** |

两边各有 `.venv`。权威文档顺序见客户端 `AGENTS.md`；状态见 `FINAL_REPORT.md`；**今天全部改动详见 `CHANGELOG.md` 顶部 2026-07-04 四轮条目（必读）**。

## 2. 今天（07-04）四轮工作摘要

1. **第一轮**：登录真机修复。两个根因：① `index.html` 有两个 `submitLogin`（旧版覆盖新版）；② webview 模式 Tk mainloop 永不运行，所有 bridge/后台线程碰 `tk.StringVar` 必抛 `RuntimeError` → 新增线程安全 `WebviewVar` 系列全量替换（35 处）。
2. **第二轮**：登录后链路全面打通。五类断链：① 会话恢复 worker 不释放 `worker_running`（busy 泄漏，所有动作静默失效）；② 43 处 Tk `messagebox` 全失效 → 统一 `notify_user`（日志+toast）；③ 启动时 evaluate_js 与页面加载竞态锁死 → `webview_ready` 门闩；④ toast 登录后不可见 → 全局浮动 toast；⑤ 算力"待刷新"误导 → "无可用权益"。另：登录失败英文 message 中文映射 `localize_server_message`。
3. **第三轮**：发现**客户端按合同、后台按需求草稿实现，路由完全对不上** → 子代理把后台全部对齐合同（`COMMERCIAL_BACKEND_API_CONTRACT.md` 为权威），新增 16 项跨项目集成测试（用客户端 `commercial_api.py` 构造器直打后台 TestClient）。修两个真机级 bug：① `opener=urlopen` 误传（timeout 被当 data，Key 校验对生产也必炸）→ `trusted_urlopen`；② 同步 bridge 里 evaluate_js 重入死锁 → **异步 JS 推送队列 `_push_js`**（log/sync_webview_state/push_webview_toast 全部队列化）。搭好本地联调环境（见 §4）。
4. **第四轮**：五个 Agent（Codex/ClaudeCode/OpenClaw/Hermes/Gemini-agy）全部真机走通 **权益→门禁→预占→安装→配置→真实 CLI 中文对话验收→扣次→交付矩阵**；失败不扣次守卫验证生效。修 agy 探针 `-m`→`--model`。后台补第 4 类佣金事件 `tool_order_paid`。代理中心用本地后台真数据走通（申请→审批→客户端同步 L1 快照）。

## 3. 合同 5 个歧义点（已全部决议，用户知情）

1. `tool_order_paid` 第 4 类佣金事件 → **已实现**（与 paid_activation 同构、比例独立配置、默认 0 未配置不允许启用）
2. 提现请求无支付宝账号字段 → 放宽可空，人工审核补充
3. 管理员批准提现无流水号字段 → 优先 `payout_reference`，缺省 reason 兜底
4. 通讯软件回调无机器人账号 ID → 平台+聊天对象必配、账号可选
5. 账本动作命名 → 按合同 `freeze/release/reverse`

## 4. 本地联调环境（已验证可复现，当前已关闭）

三个终端：

```powershell
# ① 后台（8300）
cd C:\Users\Administrator\Documents\codex\胖虎AI后台管理系统
$env:PANGHU_ADMIN_TOKEN='dev-token'
.venv\Scripts\python.exe -m uvicorn app.main:app --port 8300

# ② 联调网关（8299）：mock 登录/激活/Ed25519 签名清单(权益)/config-sessions/
#    verify-owner/三种模型接口(OpenAI+Anthropic+Gemini,含SSE)，其余 /api/* 反代后台并注入 X-Panghu-User
cd C:\Users\Administrator\Documents\codex\胖虎AI客户端
C:\Users\Administrator\Documents\codex\胖虎AI后台管理系统\.venv\Scripts\python.exe scripts\local_integration_server.py

# ③ 客户端（沙箱三件套必须齐，否则会写真实 Agent 配置！Hermes 用 LOCALAPPDATA）
$sandbox='C:\Users\Administrator\Documents\codex\胖虎AI客户端\outputs\dev_home'
$env:PANGHU_DEV_BASE_URL_OVERRIDE='http://127.0.0.1:8299'
$env:PANGHU_DEV_MANIFEST_PUBLIC_KEY_FILE='C:\Users\Administrator\Documents\codex\胖虎AI客户端\outputs\dev_integration\dev_manifest_public.pem'
$env:USERPROFILE=$sandbox; $env:LOCALAPPDATA="$sandbox\AppData\Local"; $env:APPDATA="$sandbox\AppData\Roaming"
.venv\Scripts\python.exe src\panghu_ai_client.py
```

- 联调账号任意（如 `dev-buyer@local.test` / 任意密码），网关放行一切登录
- 两个 `PANGHU_DEV_*` 环境变量是客户端联调开关，**生产构建不设置则行为与原来完全一致**；覆盖生效时窗口标题会带【本地联调】
- CDP 调试：客户端进程加 `$env:WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS='--remote-debugging-port=9223'`，用 playwright `connect_over_cdp` 驱动 UI（本会话全部真机验证都这么做的，参考 outputs/integration_*.png 截图证据）

## 5. 重要坑（新会话务必知道）

1. **webview 模式绝不能在 js_api 回调里同步 evaluate_js**，也不能碰 Tk 对象 —— 一切前端推送走 `self._push_js()` 队列；一切用户提示走 `self.notify_*()`。新增 bridge 方法请遵守。
2. **沙箱三件套**：只沙箱 USERPROFILE 不够，Hermes 走 LOCALAPPDATA（首轮已误写并用备份恢复，当前真实 Hermes 配置是干净的）。
3. 本机真实 Codex 处于**官方直登模式**（用户自己的 ChatGPT 账号），别点客户端里的"切到普通模式"等按钮，会改写真实 `~/.codex`。
4. `src/commercial_manifest_public_key.py` 是构建注入的占位文件（空 key），会遮蔽 PYTHONPATH——所以联调用 `PANGHU_DEV_MANIFEST_PUBLIC_KEY_FILE` 文件覆盖。
5. 沙箱等价测试 ≠ 真机通过：本会话反复发现"沙箱绿、真机炸"（Tk、opener、agy 参数），改动后务必 `--self-test` + 全量 pytest + 尽量 CDP 真机走一遍。
6. 硬边界：别 `git reset --hard`；两仓均有大量未提交改动且**未 commit**（用户没让提交）；删文件先征得同意；打包发布叫停中。

## 6. 待办（按优先级）

1. **生产部署（唯一大项，需用户明确批准）**：把后台管理系统部署到腾讯云服务器（aitokenapi.cc 所在机器），Nginx 加转发、配 MySQL/正式密钥、每小时 cron 调 `/api/admin/agent/ledger/mature`。**必须先读 hub 的 `PRODUCTION_LOCK.md`/`SERVER_WEBSITE_MAINTENANCE.md` 并走锁流程、备份先行**。用户已被解释过什么是生产部署（客户端是本地安装包，服务端程序必须上服务器真买家才能用）。
2. 生产侧数据打通：真买家权益/签名清单需要中转站或后台系统在生产签发（生产签名公钥经构建脚本注入客户端）。
3. 增值业务目录：客户端底座已就绪（签名清单 `value_added_services` 字段），待服务端提供目录数据。
4. 小遗留：webview 模式"发现新版本"只提示不自动更新（待前端确认框）；pywebview 启动时对 `WebviewApi.app` 深度反射的递归告警日志（不致命，建议把公开属性 `app` 改 `_app` 消除）。
5. 打包发布：等用户解除叫停后走 `scripts/build-windows-exe.*` + 生产公钥注入。

## 7. 环境备忘

- 用户账号 `206659584@qq.com`（生产 aitokenapi.cc，该账号当前无权益，故顶栏显示"无可用权益"是真实状态）
- 用户是 Windows 11，PowerShell；用户偏好：最大自主权限、少问；但生产/网站变更必须先问（见全局 CLAUDE.md 与 memory）
- 三 agent 协作 hub：`C:\Users\Administrator\.codex-claude-code-hermes-hub\`（涉及胖虎AI服务器/网站时按全局 CLAUDE.md 读对应文档）
- `outputs/` 是可重建产物不入库；联调密钥在 `outputs/dev_integration/`（测试专用，勿用于生产）
