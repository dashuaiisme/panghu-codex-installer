# 胖虎AI客户端 · 交接说明（2026-07-07，Claude）

> 给下一个窗口/agent：**先读这份**，尤其新加的 **§0.5「本会话最新状态」——那是当前真相**（含客户端分层拆分、当前生产镜像、本会话全栈审计结论、3 个已知非 bug）。本文件记录 2026-07-06 夜起至 07-07 多轮的全部改动 + 服务端上线情况 + **千万别按旧的改回去的点**。§1/§3/§7 是更早的分轮快照，与 §0.5 冲突处以 §0.5 为准。

## 0. 一句话
桌面客户端（WebView2 / pywebview 外壳，前端单文件 `src/ui/index.html`，后端 `src/panghu_ai_client.py` + 3 个拆分模块），面向外部买家。买家登录 → 填 Key → 安装/配置各 AI 助手（Codex/ClaudeCode/OpenClaw/Hermes/Gemini）走胖虎AI中转站（aitokenapi.cc）。配套服务端后台在**另一个仓** `胖虎AI后台管理系统`（panghu-admin，非 git 仓）。

## 0.5 本会话最新状态（2026-07-07 续，Claude Code Opus 4.8）—— 全盘扫描前先看这段

**这段是当前真相；下面 §1/§3 的部分数字是更早快照，以本段为准。**

### 客户端后端已分层拆分（大文件降体量，行为零变化）
`panghu_ai_client.py` 顶部 `from <mod> import *` 回引，**所有引用点不变**。当前 `src/` 分层（依赖自下而上）：
| 模块 | 行数 | 内容 |
|---|---|---|
| `panghu_constants.py` | 296 | 集中常量（App 标识/服务端 URL/通讯软件·增值业务选项/UI 设计 token/FLOW_STEPS/MODULE_* 导航文案） |
| `panghu_agents.py` | 211 | 数据模型 dataclass（AgentMode/AgentSpec/…）+ CodexConfigMode 枚举 + AGENTS 注册表 + 交付 playbook |
| `panghu_paths.py` | 124 | 全部文件路径助手（仅依赖标准库、零业务耦合） |
| `panghu_ai_client.py` | 8868 | 主逻辑（InstallerApp、WebviewApi 桥、安装/配置/验收流程）|
- 拆分前 9464 行 → 主文件 8868（另有更早一轮把 tkinter 死代码 11289→9463 净删 1826 行）。
- 剩余耦合较深的簇（密钥 DPAPI / 登录存储 / 格式化助手，且 `device_fingerprint` 在文件深处被前向引用）**未再拆**：要续拆须先把 `device_fingerprint` 等原语下沉到地基模块。见 `AGENTS.md §6`。

### 测试基线（当前真实）
- 客户端 **`pytest tests -q` = 367 passed, 192 subtests 全绿**。（不是旧 §1/§7 的 370/371；差额是把 5 个"钉死 tk 代码"的测试删掉、新增 1 个删除锁定测试 = 净 -4。）
- 服务端 **`pytest tests -q` = 164 passed 全绿**。

### 服务端当前生产镜像已更新
- **当前 LIVE = `panghu-admin:20260707-cleanup2`**（本会话部署，非 §3 写的 wap-autoaccept）。内容=安全清理批：删死函数 `deployer_service.sellable_products`、`vas_routes` 4 端点补 `{success,data}` 信封、抽 `payment.new_out_trade_no()` 去重、`models/payment` 设计意图注释 + T+30 订正。**资金分发路径未重排**（有意保留）。逐字节核对 tested==live 零差异、备份+回滚点齐备。回滚镜像 `panghu-admin:20260707b-commlink-alipay`。

### 本会话做了一次全栈 bug 排查（多视角，结论：未发现活 bug）
> 4 个并行审查 agent 撞账号周限额中止，改由主 loop 逐维直接查。已验证以下面，**全盘扫描可据此聚焦到"我没覆盖到的角"，避免重复劳动**：
- **服务端资金链**（alipay/payment/commission/settlement）逐行读：验签→app_id→行锁→金额→幂等→先履约后标 PAID；佣金整数 bps 无浮点；提现 FIFO 行锁+**边界拆分防金额销毁**+审批只结算仍 available 行防退款后照付。✅ 生产级、无 bug。
- **鉴权** deps.py：完全 fail-closed、timing-safe 比较、时区归一。✅
- **webview 桥契约**：36 个前端 `pywebview.api.X` 调用 vs 55 个 WebviewApi 方法做差集 = **零缺失/零死按钮**；动态分发（codex 模式切换）、actionMap、`copy_logs→copyLogs()` 特判全部解析。✅
- **tkinter 删除残留**：AST 扫出 21 处 `self.X` 无定义引用，**逐一确认全被死分支/webview 早退/hasattr 保护**，无运行时崩溃。✅
- **客户端支付**：`order_create → /api/deployer/orders → build_wap_pay_url` = WAP 扫码，符合设计（非 page.pay）。✅
- **机械全扫**：两库全 `.py` 编译通过；前端 29 个 `onclick` 处理器全部有定义；三个 `import *` 无名字冲突。✅

### 3 个"已知非 bug"观察（**别当 bug 反复修**）
1. `copy_logs`（Python `InstallerApp.copy_logs`）：WebView 模式空转、tk 路径有测试 `test_copy_logs_sanitizes_clipboard_text`。**不是死代码、不能删**（删了破测试）；实际复制由前端 `copyLogs()` 完成。
2. 通用 `/api/payment/orders`（`create_payment_order → build_page_pay_url`）走**电脑网站支付**，与工具/通讯软件订单的 WAP 不一致；桌面客户端不走此端点（走 `/api/deployer/orders`）。属旧/备用路径，未被客户端消费。**latent 项：下次服务端"有功能变更"部署时再统一到 WAP 或标注弃用，不为它单独动生产。**
3. `alipay.py` 时间戳用服务器本地时区；生产已成功扣过真单说明实际 OK，迁移服务器时确认时区仍 Asia/Shanghai 即可。

### 本会话未改动的生产代码
除上面已部署的 cleanup2 外，本会话**没有再动任何生产/客户端业务逻辑**——只做了拆分（行为不变）、文档、审计。清代码逻辑保持绿。

### 给全盘扫描的「建议重点复核角」（本会话信心高但未逐行，值得再独立验一层）
1. **服务端 `app/commlink.py`（613 行）通讯软件自动验收状态机**：本会话只抽查了幂等键/唯一约束/`IntegrityError` 兜底/行锁与 `charge_status`（unpaid/paid/manual_review/refunded）流转，未逐行走 `auto_accept_if_ready` / `submit_acceptance` / `api_record_outbound` 的完整派生链（`source_event_id=csl:{platform}:{msgid}` 幂等、`begin_nested` savepoint 嵌套）。重点看：并发/重放下会不会重复验收或重复扣费、平台回调字段缺失时的分支。
2. **各 Agent 配置写入模板**（`panghu_ai_client.py` 的 `build_*_env`/`build_config`/`merge_config`、`install_*`）：openclaw/hermes/agy 的 config 文件路径、引号/特殊字符转义、覆盖既有配置的合并逻辑——离线单测覆盖了字符串，但**真机写文件 + 各 CLI 实际读取**才好验（另见 §4.3 `config validate/check` 子命令名待真终端确认）。注意已知限制：`build_config`/`merge_config` 恒写死型号、忽略传入 `model`（§4.1，是设计非 bug）。
3. **前端 `index.html` 复杂状态机边界态**：登录闸口↔控制台、四个一级模块 + 子导航 + 11 步流程的切换。本会话验了桥契约与 onclick 定义，但**多次快速切换/异步回调乱序/登录态失效中途**这类边界的状态一致性值得动态复核（`state.*` 与后端 push 的 `updatePythonState` 竞态）。
4. **前后端契约三方一致性**（客户端 `commercial_api.py`/`panghu_ai_client.py` 调用 ↔ 服务端 `app/api/*` 实现 ↔ `docs/COMMERCIAL_BACKEND_API_CONTRACT.md`）：本会话验了支付与 vas 信封两条主线，**其余端点的字段名/大小写/金额单位(元vs分)/枚举值/分页参数**建议做一遍全量对表（原计划的第 4 个审查 agent 就是干这个，撞限额没跑成）。

## 1. 基线
- 分支 `codex/panghu-client-worktree-closeout`，**HEAD = `0e548e3`**，`pytest tests -q` **370 passed** 全绿。
- 本轮提交：`d2299d1`(gpt型号) → `d76eb90`(claude型号) → `0e548e3`(安装/配置拆分)。更早本任务链：`bf06db0`/`e349431`/`0067d70`/`665273c`/`924314b`。
- 客户端**仍未打包发布**（冻结中）。以下客户端改动随下次发布生效。

---

## 2. ⚠️ 千万别改回去（后来 agent 最容易踩的反向坑）

### 2.1 模型型号：用新的，别用旧的
| Agent（格式） | **正确型号(现用)** | ❌ 旧的错的 |
|---|---|---|
| Codex / OpenClaw / Hermes（openai 格式） | **`gpt-5.5`** | ~~gpt-5.4~~ |
| ClaudeCode（anthropic 格式） | **`claude-opus-4-8`**（中划线！） | ~~claude-opus-4.8（点）~~ |
| Gemini / agy（gemini 格式） | `gemini-3.5` | —（中转站尚未接入 gemini） |

- 依据：用用户真实中转站 key 实测——`gpt-5.4`/`claude-opus-4.8`(点) 都报 `model_not_found`；`gpt-5.5`、`claude-opus-4-8`(中划线) 实测回复"测试成功"。
- 全仓已无 `gpt-5.4`/`claude-opus-4.8`。**别再填回旧型号。**
- 中转站分组：Claude 分组(cc1)有 claude-opus-4-8 等；GPT 分组已由用户接上 `gpt-5.5`（实测通过）；**Gemini 分组用户说"还没接入"**，先别指望 gemini 真机通。

### 2.2 「安装」与「写入配置」是两个独立步骤，别再合并
- 现在：**第4步「安装」= 只装软件**（后端 `start_install`/`_install_worker`，不写配置、不预占/扣次）；**第5步「写入配置」= 真写配置+验收**（按钮调 `start_deploy`）。
- 旧版把二者绑在 `start_deploy` 一把梭，且第5步只显示配置健康面板不写配置——**已修，别改回去**。
- **扣费时机没变**：仍在配置验收通过后（`_deploy_worker` 计费逻辑一行没动）。`_install_worker` 绝不能加预占/扣次逻辑。

### 2.3 支付方式：支付宝 WAP，别用旧 page.pay
- 工具订单走**手机网站支付**：`alipay.trade.wap.pay` / `QUICK_WAP_WAY`。服务端返回字段仍叫 `payment_url`，**不返回 qr_code**；客户端本地用 `qrcode`+`PIL` 把 `payment_url` 编码成二维码（`build_payment_qr_data_url`）。
- **别再按电脑网站支付 `alipay.trade.page.pay`(FAST_INSTANT_TRADE_PAY) 的旧数据来**（有 ¥50 单笔限额）。

### 2.4 通讯软件验收：服务端自动验收，客户端别提交内部字段
- 第10/11步：客户端只轮询 `acceptance_status`/`charged`，**不要**让买家手填 `source_event_id`/`inbound/outbound_platform_message_id`/`agent_response_digest`/`evidence_url`。这些由服务端从真实平台回调派生。

---

## 3. 服务端（panghu-admin）已上线的东西
> 仓：`C:\Users\Administrator\Documents\codex\胖虎AI后台管理系统`（**非 git 仓**，改动只在盘上）。生产机 `panghuai-server`(43.165.168.188, aitokenapi.cc)，容器 `panghu-admin` @127.0.0.1:8300。

- ⚠️ **此行为更早快照。当前 LIVE 已是 `panghu-admin:20260707-cleanup2`（见 §0.5）。** 本节以下 wap-autoaccept / auto-accept 的功能内容仍准确，只是镜像标签已被后续 cleanup2 覆盖。
- （历史）`panghu-admin:20260707-wap-autoaccept` 曾上线（2026-07-07）。回滚镜像 `panghu-admin:20260705-r6`。
- #5 WAP 支付：`app/alipay.py` 加 `build_wap_pay_url`；`app/deployer_service.create_tool_order` 改用它。
- #4 服务端自动验收：`app/commlink.py` 加 `auto_accept_if_ready`（Adapter 成功出站后按真实入站消息派生 `source_event_id=csl:{platform}:{msgid}` 幂等自动验收+扣费）；`api_record_outbound` 触发；`_session_data` 暴露 `charged`。
- **生产验证均已通过**：真实 ¥0.10 支付宝 WAP 扫码→到账→履约→权益 active；模拟带签名平台回调→自动验收 `charged=True`/`accepted_by=server-auto`。
- 上线/回滚/验证细节见 hub `PANGHUAI_CHANGE_LOG.md` 2026-07-07 各条。**部署要走生产锁 + 用户批准**（见 hub `PRODUCTION_LOCK.md`、本仓 `RUNBOOK.md`）。

---

## 4. 已知坑 / 限制（别当 bug 反复"修")
1. **`build_config`/`merge_config` 忽略传入的 `model` 参数**，恒写死型号字符串（现为 `gpt-5.5`）。即买家在 UI 选别的型号也不会生效。本轮只把写死值改成 gpt-5.5；「按买家选择写型号」是**后续增强**（要把模板改成 f-string 用 `{model}`，并同步 `panghu_ai_client.py` 内联测试 `merge_config(...,"bad","bad")==expected` 那几处 + `test_agent_playbooks`）。
2. **撤单退款不自动退钱**：`reverse_tool_order` 只把订单置 `refund_pending` + 回收权益，**不调支付宝退款 API**。真退款要在支付宝商户后台手动做。
3. **`config validate`/`config check` 子命令**：客户端会跑 `openclaw config validate`、`hermes config check`（`panghu_ai_client.py:4236/4299`）。开发机上这两 CLI 是 TUI 型，非交互终端探测会启动它们的 Agent，子命令名是否被接受**待真交互终端确认**；若名字不符是**客户端可改项**。

---

## 5. 剩余待办
| # | 事项 | 状态 |
|---|---|---|
| 1 | 真机验安装（openclaw/hermes/agy） | 开发机 `--version` 均通过、型号名对得上；`config validate/check` 子命令待真终端确认；中转站端到端 codex(gpt-5.5)/claude(claude-opus-4-8) **已实测通过**，gemini 待中转站接入 |
| 2 | 购买写死 codex（后端） | ✅ 已修（`a508535`：`start_buyer_create_order` 从商品快照取 agent/mode） |
| 3 | client 模式后端死路 | ✅ 已修（`ea5c248`：`verify_client_scope_delivery_evidence`，客户端"装好+配置写入"即合格扣次） |
| 4 | 通讯软件验收字段 | ✅ 改服务端自动验收并上线；剩"真实平台真机回调"联调（已用模拟签名回调在生产验证通过） |
| 5 | 客户端打包发布 | ⏳ 仍冻结/未发布 |

## 6. 三处东西在哪
- **客户端代码**：本仓（git，分支 `codex/panghu-client-worktree-closeout`）。
- **服务端后台**：`胖虎AI后台管理系统`（非 git，改盘 + 走生产锁上线）。
- **跨工具协作/上线台账**：`C:\Users\Administrator\.codex-claude-code-hermes-hub\`（`PANGHUAI_CHANGE_LOG.md` 全量、`STATUS.md` 摘要、`PRODUCTION_LOCK.md` 生产锁）。

---

## 7. 第二轮：多视角验收 + 全量修复（2026-07-07，commit `f17e05e`）
5 视角审查(买家端到端 / 跨仓合同 / 前端 / 客户端后端 / 服务端资金安全)后修的真 bug。**别改回去**：

### 已修（客户端，已提交 `f17e05e`，371 测试绿）
| 级别 | 问题 | 位置/做法 |
|---|---|---|
| P0 | 运行日志满屏 `[object Object]`(买家看不到结果) | `appendPythonLog` 解析 `{t,c,m}` 对象 + `renderLogs`/`seedLogsFromState` 从 `state.logs` 接历史日志 |
| P0 | **Codex 没真装也扣次**(假交付) | `_deploy_worker` codex 段：先取真实 `install_agent` 结果 `codex_binary_ok`，没装上则 INSTALL≠PASS、跳过网关探测、不扣次(+回归测试 `test_deploy_worker_does_not_charge_codex_when_binary_not_installed`) |
| P0 | 扫码付款写死 codex(选别的 Agent 白付) | `refresh_recommended_agent_product` 改按 `selected_agents()[0]` 取商品，保留手填 |
| P1 | `start_install` 无并发护栏 | 开头加 `if self.worker_running: return` |
| P1 | 第7步"连通测试"误调完整付费部署 | actionMap `"7"`→`copy_logs`，兜底动作也改不计费 |
| P1 | 增值业务数据 XSS | `escapeHtml`/`safeHttpUrl`/`jsStringInAttr` 转义 `meta[0/1/2]` |
| P1 | 侧栏跳步绕门禁 + 假"已完成✓" | `switch_subnav` 走 `go_to_step` 门禁；`agentStepComplete()` 按真实信号判完成 |
| P2 | `success_count` 虚高 / 下拉小字自相矛盾 / 环境检测虚假"隧道✓" | 分别按真实 CONFIG_WRITE / `agentModeOptions` / 诚实文案修正 |

### 已修（服务端 panghu-admin，**本地改动、尚未部署**，163 测试绿）
- 5 个创建接口(提现/下通讯软件单/平台授权/申请代理/提交验收)幂等键并发竞态：加 `IntegrityError`/`begin_nested` savepoint 兜底，重复提交返回既有结果而非 500。`submit_acceptance` 用 savepoint 因它会被 `auto_accept_if_ready` 嵌套调用。
- 3 个 admin 列表接口 `status` 参数加 membership 校验(非法值 422 而非 500)。
- 订正 `commission.py`/README 的"T+30"旧注释 → "结算等待期以管理员后台设置为准，默认 T+7"。
- ⚠️ **这批服务端改动要走生产锁 + 用户批准才能上线**(见第 3 节 / hub `PRODUCTION_LOCK.md`)；当前生产仍是 `panghu-admin:20260707-wap-autoaccept`(不含本批)。

### 按用户决策"设计如此，不改"
- 客户端模式(Claude/Gemini 客户端)"装好 + 配置写入即验收合格、不做联网验收"——软件无法对这些 Agent 客户端做联动测试。见 [[panghu_client_mode_primary_decision]]。
- 撤单退款走**管理员后台人工**(撤单 + 追回佣金)，客户端不做退款功能。
- 结算等待期由**管理员后台设置**(默认 T+7)，代码不改。
- 不下架 gemini(勿删 claude/gemini 客户端选项)；gemini CLI 探测失败不扣次、客户端模式按上面的验收口径。

### 待你决策 / 后续（非本轮代码 bug）
- **通讯软件(#4)客户端内无法自助付款**：服务端不给通讯软件订单返回 `payment_url`，只能后台人工把订单置"复核通过"才开会话。要不要给它接支付宝(像工具订单)还是明确走人工预售？——**涉及支付，改前请示你**。
- 发布前必核对(两仓代码看不出)：`commercial_manifest_public_key.py` 注入与服务端私钥配对的公钥；`PANGHU_DEPLOYER_TOKEN_SECRET` 与主站 `SESSION_SECRET` 一致。上线冒烟各验一次。
- 运营须知：要交付完整配置的工具商品，库里 `delivery_scope` 必须设 `full_config`，否则买家付了钱被客户端拦。
- 真机验 openclaw/hermes/gemini 安装 URL 与 `config validate/check` 子命令名(见第 5 节待办)。
- `/api/referrals/bind` 客户端有构造器但无 UI 接线、服务端无该路由(潜伏，未接线不炸)；将来要么服务端实现、要么删客户端动作，邀请归因现走 `/api/agent/apply` 的 `invite_code`。

---
_本轮由 Claude Code (Opus 4.8) 执行。客户端改动已提交并全绿；服务端改动本地全绿但**待部署**；真机端到端(真实付款/真实平台回调/gemini 中转)按各表状态。_
