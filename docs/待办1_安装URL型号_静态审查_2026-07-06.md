# 待办 #1 · openclaw/hermes/gemini 安装 URL 与型号名 · 静态审查

> 目的：交接说明待办 #1「真机验安装」在沙箱/无真机时无法端到端验证。本文用**只读**方式（HTTP 可达性 + 脚本内容 + npm registry + 代码常量比对）把风险面收窄，区分「已静态证伪风险」与「仍需真机验证」。
> 时间：2026-07-06。基线 HEAD `e9ce225`（本轮 #2 修复未提交时）。审查者：Claude Code。方式：`curl` 只读探测，未执行任何安装脚本、未改生产。

## 结论速览

| Agent | 安装入口 | 可达性 | 实际返回 | verify 命令 | 判定 |
|---|---|---|---|---|---|
| OpenClaw | `npm i -g openclaw@latest` | ✓ | registry `latest=2026.6.11` | `openclaw --version` | 入口真实 |
| OpenClaw | Win `iwr https://openclaw.ai/install.ps1 \| iex` | 200 | 真 PowerShell 脚本(`# OpenClaw Installer for Windows`) | 同上 | 入口真实 |
| OpenClaw | *nix `curl https://openclaw.ai/install.sh \| sh` | 200 | 真 bash(`#!/bin/bash set -euo pipefail`) | 同上 | 入口真实 |
| Hermes | Win `irm .../NousResearch/hermes-agent/main/scripts/install.ps1` | 200 | raw.githubusercontent 真脚本 | `hermes --version` | 入口真实 |
| Hermes | *nix `curl https://hermes-agent.nousresearch.com/install.sh \| bash` | 200 | 真 bash 脚本 | 同上 | 入口真实 |
| Gemini/agy | Win `iwr https://antigravity.google/cli/install.ps1 \| iex` | 200 | 真 PowerShell(`$ErrorActionPreference="Stop"`) | `agy --version` | 入口真实 |
| Gemini/agy | *nix `curl https://antigravity.google/cli/install.sh \| bash` | 200 | 真 bash(`Antigravity CLI - Unix Bootstrapper`) | 同上 | 入口真实 |

**关键正向证据**：三个安装脚本正文里都出现了客户端期望的 verify 二进制名——antigravity 脚本含 `agy`、hermes 脚本含 `hermes` + 大量 `--version`、openclaw 脚本含 `openclaw` + `--version`。即「装完应产生的命令名」与 `AgentSpec.verify_command` 一致，没有明显对不上的写死。

> 相比交接说明的担忧「URL 与型号名是否真实可用（不对会一装即败）」——**URL 层面已基本证真**：域名解析、HTTP 200、且返回的是真脚本而非营销站 SPA 的 HTML 兜底（否则 `iex`/`| sh` 会当场炸）。这一层风险显著下降。

## 型号名比对（前端预填 → 后端路由）

- 前端 `KEY_FORMATS`（`src/ui/index.html`）预填：OpenAI 兼容=`gpt-5.5`、Anthropic=`claude-opus-4.8`、Gemini=`gemini-3.5`。与交接说明「最新型号」一致，Gemini 用 `gemini-3.5`（非 pro）符合「中转站用不了 pro」。
- 后端按格式取 key/model：`key_model_for_agent()` → `AGENT_KEY_FORMAT`（openclaw/hermes=`openai`，gemini_agy=`gemini`）。买家在 step1 预填的型号会随对应格式下发，**不会**掉回 `DEFAULT_MODEL`。
- `DEFAULT_MODEL="gpt-5.4"` 仅作 openai 格式**空值兜底**（买家清空型号才用到）。改它会破 `test_agent_playbooks` 矩阵断言——保持不动。
- 写入位置：OpenClaw→`build_openclaw_config`（网关 `CODEX_BASE_URL`=`https://aitokenapi.cc/v1`，模型 `panghuai/<model>`）；Hermes→`build_hermes_config`（同网关，chat_completions）；Gemini→`~/.gemini/.env` 的 `GOOGLE_GEMINI_BASE_URL`=`https://aitokenapi.cc`、`GEMINI_MODEL=<model>`。

## 仍需真机验证（本文无法覆盖）

这些是 `curl` 只读探测证明不了的，必须在真 Windows/Mac + 活的 aitokenapi.cc 上跑：

1. **装完是否真出现可用命令**：脚本 200 ≠ 装成功。需真机跑安装后 `openclaw --version` / `hermes --version` / `agy --version` 均返回 0。这三者**无手动安装兜底**，装失败即 step 部署失败。
2. **配置校验命令是否存在/接受**：`openclaw config validate`、`hermes config check` 是代码里写的校验钩子。若这两条子命令名与真实 CLI 不符，会走 `RuntimeError` 恢复备份路径 → 验收失败。需真机确认子命令名。
3. **中转站端到端**：`gpt-5.5`/`gemini-3.5`/`claude-opus-4.8` 这些型号 aitokenapi.cc 是否真的开了、这三个 Agent 的请求格式（openai/gemini/anthropic）中转站是否放行。需真 Key 打一次最小对话。
4. **平台差异**：antigravity/hermes 的 macOS 分支未探测（脚本可能按 OS 分发不同二进制）；Mac 上的 `agy`/`hermes` 安装未验证。

## 建议

- 待办 #1 的「URL 真实性」子项可视为**已用只读证据收窄**（入口全部真实、二进制名对得上）；剩余是「真机安装 + 中转站放行」两类，须你在真机按上面 1–4 逐条跑。
- 若真机 `config validate`/`config check` 子命令名对不上，属**代码可改项**（改 `install_openclaw_config`/`install_hermes_config` 里的子命令），回流本窗口即可。

---
_只读审查，未执行安装脚本、未改任何生产/配置。_
