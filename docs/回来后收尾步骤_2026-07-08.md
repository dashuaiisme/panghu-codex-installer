# 回来后的收尾步骤（2026-07-08，给 panghu）

> panghu 出门前批准了"自动推进到上线"。但你走后，**桌面工具变得很不稳定**（微信不停弹窗抢焦点、资源管理器地址栏串字、脚本双击点不动），我连一个提交脚本都无法可靠地跑起来。
>
> **我做了一个安全决定：不在"无人看管 + 工具不可靠 + 无法验证结果"的情况下，把未验证的改动推到你正在赚钱的生产站点。** 半吊子、无法核验的生产部署，比多等一会儿风险大得多。所以我把活儿停在"全部做好、本地验过、代码安全躺在盘上"这个干净的点，等你回来、工具能正常用时（你在的时候今天早些都能跑），我们一步步稳稳上线。

## 现状：全部做好、本地验过（没碰生产、没提交）

| 部分 | 状态 |
|---|---|
| 后台 AI订阅：商品表/订单/支付宝/批量发卡密/卡密池/客服微信 | ✅ 本地，服务端 198 测试绿 |
| 后台可视化"AI订阅"管理页（你自己加商品/改价/传图/录卡密/配客服微信） | ✅ 本地，admin.html JS node 校验过 |
| 客户端 AI订阅商城页（商品卡/数量/购买→支付宝→出卡密→打开充值工具） | ✅ 本地，客户端 367 测试绿，预览点通 |
| 客户端 接码控制台改内嵌（iframe 内嵌 sim.aitokenapi.cc + 独立窗口兜底） | ✅ 本地，预览里真站已内嵌 |
| 客户端 代理中心"成为代理"说明（三类返佣/五级/怎么升级/结算，不硬编码费率） | ✅ 本地，预览渲染正确 |
| 与 Plus 充值工具的卡密契约 | ✅ 已核对一致（工具 v2.4.0，契约未变），见 `汇总_AI订阅与Plus充值工具整链_2026-07-08.md` |

**代码都在盘上（未提交）。令牌两个别合并（最小权限，是对的）：`LICENSE_ADMIN_TOKEN`(胖虎写卡密) vs `LICENSE_SERVICE_TOKEN`(工具核销)。**

## 回来后按这个顺序（工具正常时很快）

### 第 1 步：提交客户端代码（安全，先做）
- 双击客户端根目录 `_finalize_client.bat`（会：node 校验前端 JS → 跑 pytest，绿了才 `git commit`；红了不提交）。
- 看 `_finalize_client.log`：应显示 `367 passed` + 一条新 commit。

### 第 2 步：部署 panghu-admin（AI订阅后台 + 管理页）到生产
- 这是"你能自己加商品"真正生效的一步。安全可回滚（容器替换 + 备份 + tested==live 核对），跟我之前部署 keyowner-cslfix 同一套路。
- 我们一起走生产锁：Phase A 逐字节对比确认只有 subscription 相关文件相异、无意外漂移 → Phase B 切换 + 冒烟 + 留回滚点。
- 完成后你在 `aitokenapi.cc/admin` → 左边「AI订阅」就能加商品、录卡密、配客服微信了。

### 第 3 步：部署 Plus 充值链路（卡密库存 + 买家端）
- **licenseServer（卡密库存）**：建议内网容器（不暴露公网），生成 `LICENSE_SERVICE_TOKEN`/`LICENSE_ADMIN_TOKEN`。
- panghu-admin `.env` 追加 `PANGHU_LICENSE_SERVICE_BASE_URL`（内网地址）+ `PANGHU_LICENSE_ADMIN_TOKEN`（=上面的 admin 令牌）。
- **Plus 买家端**：部署到公网子域（如 `plus.aitokenapi.cc`，需 Nginx/DNS），后台"卡密来源"设 remote 指到 licenseServer + `LICENSE_SERVICE_TOKEN`。
- 后台把 GPT Plus 商品的 `plus_tool_url` 填成买家端地址。

### 第 4 步：客户端打包发布
- 双击 `_build_all.bat`（打 Windows 包、注公钥、exe 自检、release 验收），跟之前一样。
- 你确认后再分发给买家。

### 第 5 步：真机端到端实测（★只有你能做）
- 后台建一个 GPT Plus 代充商品、录几张卡密。
- 客户端登录 → 增值业务 → AI订阅服务 → 买 → **真实支付宝小额付款** → 出卡密 → 打开充值工具 → 输卡密 + **你的真实 ChatGPT session** → 自动化跑通 Plus 升级（首次可能要微调选择器）→ 卡密核销 → 同卡再用应被拒。
- manual_qr 商品：付款后应弹你配的客服微信。

## 需要你本人的点（我替不了）
- 真实支付宝付款、真实 ChatGPT 号、真实 Stripe 扣款卡的端到端实测。
- 公网子域（plus./license.）的 DNS / Nginx（碰全局反代，我不在你在场时不动）。
- 客户端正式对外分发的最终拍板。

_一句话：东西全做好了、验过了，就差"你在场时把它们稳稳部署 + 用真号真钱验一遍"。我没有在工具不可靠、无法核验时硬推生产——这是对你生产站点负责。_
