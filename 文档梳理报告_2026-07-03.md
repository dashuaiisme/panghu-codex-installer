# 文档梳理报告

日期：2026-07-03
范围：当前仓库根目录文档和 docs 关键手册

## 0. 当前有效结论

本仓库不是空目录，也不是缺源码状态。当前真实项目入口是：

```text
C:\Users\Administrator\Documents\codex\胖虎AI客户端
```

当前可观察事实：

- `src/`、`tests/`、`scripts/`、`docs/` 和 `.git` 均存在。
- 当前客户 UI 入口为 `src/ui/index.html`。
- 当前未见 `outputs/`；截图只能写成历史曾生成或可通过脚本重建，不能写成当前证据路径可用。
- 当前未见 `.venv`；`.venv` 下网站入口前提检查只能作为历史记录，复验需先重建环境。
- 当前不是整体交付状态；真实网页登录、支付、API Key、客户端 scope、代理中心服务端、连接通讯软件、三端客户包、Release 和 `latest.json` 均未完成。
- `commercial_release_acceptance.py --json` 最近记录仍为 `WARN`，不得进入发布。

## 1. P0 问题：过期报告口径失真

初版梳理报告曾把目录状态写成源码缺失或目录为空，这已经与当前仓库事实冲突。该口径会误导后续窗口跳过真实代码、脚本、测试和 UI 入口。

整改结论：

- 本报告重写为当前有效报告。
- 后续不得再引用“目录为空 / 代码缺失”作为项目事实。
- 项目状态以当前仓库、`AGENTS.md`、产品手册、技术维护手册、`FINAL_REPORT.md` 和 `TESTING.md` 为准。

## 2. P1 问题：验证数字和证据状态重复冲突

同一类验证结果曾在 `FINAL_REPORT.md`、`TASKS.md`、`TESTING.md`、`RUNBOOK.md`、`PLAN.md`、`TASK_GRAPH.md` 多处重复，且把不同时间、不同命令来源的记录混写成当前结果。

整改结论：

- `TESTING.md` 建“历史验证记录”表，按日期/来源分开记录旧记录、后续记录和最近记录。
- 其它文件只引用 `TESTING.md`，不再重复具体通过数量。
- 未在本轮重新执行的命令，只写“历史记录 / 最近记录，需复验”，不写成当前已复验。

## 3. P1 问题：截图和 `.venv` 证据口径不准确

当前文件系统中未见 `outputs/` 和 `.venv`；旧文档仍写成当前截图路径和 `.venv` 检查结果可直接作为证据。

整改结论：

- `outputs/` 改为“可重建输出目录”；历史截图不得当作当前存在证据。
- `.venv` 下网站入口检查改为历史记录；后续复验需先重建环境。
- 当前 B 级视觉验收待用 `python scripts\capture_ui_preview.py` 重新生成证据。

## 4. P1 问题：旧前端状态表述混乱

旧文档把“旧 WebView shell / 旧截图 / 旧输出清理”写成“当前 WebView 前端删除”，这与当前 `src/ui/index.html` 存在冲突。

整改结论：

- 当前客户 UI 入口仍是 `src/ui/index.html`。
- 已清理的是旧 WebView shell、旧截图和旧输出证据。
- B 级视觉验收不再引用旧截图，需用当前脚本重新复验。

## 5. P1 问题：代理中心和连接通讯软件完成态过度

旧文档对代理中心、连接通讯软件使用了容易被理解为功能完成的措辞。当前真实状态是：

- 代理中心：本仓可控范围内已有文档合同、离线守卫、请求构造和测试边界；真实服务端、管理员后台、生产数据和结算闭环未完成。
- 连接通讯软件：本仓可控范围内已有产品口径、服务合同和离线守卫；真实客户端入口、后端状态机、平台通道、支付账本和生产验收未完成。

整改结论：

- 所有状态描述必须区分“文档合同 / 离线守卫 / 请求构造”和“真实服务端 / 生产闭环”。
- 本地离线合同或 mock 守卫不得写成真实业务完成。

## 6. P2 问题：文档职责重复

权威顺序、验证命令、当前状态、发布边界在多个文件重复，维护成本高。

整改结论：

- 产品事实继续以 `docs/PRODUCT_MANUAL_SINGLE_SOURCE_OF_TRUTH.md` 为准。
- 技术维护、接口、构建和发布以 `docs/TECHNICAL_MAINTENANCE_MANUAL.md` 为准。
- 验证数字以 `TESTING.md` 的历史记录表为准。
- 当前真实状态以 `FINAL_REPORT.md` 为准，但不重复具体数字。
- 运行命令以 `RUNBOOK.md` 为准。

## 7. 本次最小整改范围

本次只修正文档口径，不修改源码、脚本、测试、构建产物、缓存、输出目录或版本控制状态。

整改文件限定为用户允许的根目录文档。未纳入允许范围的旧 handoff 或历史材料若仍有过期表述，本次不越权修改。

## 8. 执行结果（2026-07-03 第二轮，经用户批准）

第 6 节的文档职责重复问题已通过结构合并解决：根目录文档从 23 个收敛为 12 个，合并映射和同轮修正详见 `CHANGELOG.md` 2026-07-03 第二轮记录。要点：

- 权威顺序唯一维护处：`AGENTS.md`。
- 状态/任务/阻塞唯一归口：`FINAL_REPORT.md`（吸收 PLAN/TASKS/TASK_GRAPH）。
- 验证数字唯一归口：`TESTING.md`（不变）。
- 运行+发布：`RUNBOOK.md`（吸收 DEPLOYMENT）；安全：`SECURITY.md`（吸收 SAFETY）；架构+前端+设计+后端：`ARCHITECTURE.md`；产品+蓝图：`PRODUCT.md`；交接+盘点+产品树：`HANDOFF.md`。
- 本报告为审计与执行记录，后续维护规则见 `HANDOFF.md` 第 8 节。
