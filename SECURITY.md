# 安全与边界

最后更新：2026-07-03

本文件覆盖密钥、数据、商业边界、生产边界与禁止事项（原 `SAFETY.md` 已并入本文件）。

## 1. 密钥和账号

- 权限：普通源码整理不需要生产服务器、数据库、支付后台、GitHub Release 或下载页写权限。
- API Key 只能通过用户输入或临时环境变量进入验证流程，不输出到日志。
- 不得把 API Key、部署 token、订单号、权益 ID、配置会话 ID、第三方账号密码写入日志、截图、报告或长期本地文件。
- `profile.json` 只允许保存账号提示、API Key、模型和界面偏好；不得写入可恢复买家登录 token 或部署授权 token。
- 买家会话走独立 cookie/WebView profile。
- 用户显式选择记住密码时，只允许保存本机系统加密后的 `login_accounts.json` 记录，不得保存明文密码。

## 2. 敏感信息边界

- token、Authorization、邀请码、订单号、权益 ID、配置会话 ID、下游客户明细、佣金账本明细不输出到客服日志或诊断包。
- 连接通讯软件的平台 token、机器人密钥、回调 secret、群聊 ID、平台用户 ID 和验收消息原文必须脱敏；客户说明和日志只保留必要摘要、消息 ID 或诊断码。
- 不把真实密钥、账号密码、生产敏感日志发给网页端或其他 agent。

## 3. 商业边界

客户端不得本地硬编码或自行计算：

- 商品价格、购买次数、有效期、设备数
- token 返佣、激活返佣、安装返佣
- 代理等级、下游客户数、结算金额、下游客户归因
- 商品上架状态、真实履约完成或真实交付成功

这些信息必须来自服务端页面、服务端快照或真实验收记录。

其他商业口径禁令：

- 不把 ClaudeCode、OpenClaw、Hermes 未打通项包装成完整付费交付；Gemini / agy 未接入前只允许作为官方入口或待接入状态。
- 不把代理中心写成已完成业务闭环；没有服务端合同时只能写待接入或占位状态。
- 不把连接通讯软件写成基础 Agent 配置的一部分；不得共用基础配置订单、验收记录或扣费事件。
- 不把客户断网、禁用 Key、取消平台授权、关闭机器人或删除群聊后的实时不可达自动判定为配置失败、退款或不收费。
- 未通过功能验收矩阵不得扣次或包装成交付完成。

## 4. 数据

客户数据、买家会话、代理结算、订单、权益、支付回调和通讯软件验收证据都属于高风险数据。客户端只能保存最低限度本地状态，不能把服务端账本数据复制为本地权威。

## 5. 生产边界

当前阶段不做生产变更：不碰生产服务器、数据库、GitHub Release、下载页、`latest.json`。

如果以后进入生产下载入口、服务器、数据库、支付或钱包改动，必须先按胖虎AI生产维护规则拿锁、记录计划、备份点和验证计划。

## 6. 发布安全

- 构建客户包前必须注入商业清单生产公钥。
- 客户包不得包含内部维护文档、测试、源码、签名资料或临时验证残留。
- `release/` 下的客户 zip 是本地交付物，不提交源码仓。
- 本地轻量验收 WARN 时不得发布。

## 7. 高风险操作

以下操作属于高风险，必须先有明确计划、备份或回滚说明：

- 生产服务器、数据库、支付、订单、权益、账本、代理结算变更。
- GitHub Release、下载页、`latest.json` 更新。
- 删除或替换客户包。
- 修改商业验签、公钥注入、扣次、配置会话、连接通讯软件验收逻辑。

## 8. 删除与覆盖边界

- 不覆盖或回退用户现有未确认改动。
- 不静默删除归档资料或客户交付物。
- 不删除 `docs/superpowers/`。
- 不删除 `release/`、Windows/Mac 客户 zip、exe/app 软件本体。
- 不把客户本地资料、登录信息、工作区资料、备份文件当成普通缓存处理。
- 不运行 `python scripts\commercial_release_acceptance.py --with-exe-self-test --deep-scan --json`。

## 9. 运行安全

- 风险插件或路由工具如 `ccswitch`、`codex++`、`CCR / Claude Code Router` 被检测到时，部署必须停止并提示客户处理。
- 临时 OpenAI 官网访问窗口只能短时启用，到点恢复系统代理；不得改成常驻代理。
- 连接通讯软件形成验收证据后，客户断网、禁用 Key、取消平台授权或阻断回调，不得被客户端自动解释为免单或失败。
