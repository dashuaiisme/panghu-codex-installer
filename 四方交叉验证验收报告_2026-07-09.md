# 胖虎AI客户端 · 四方交叉验证验收报告

> 生成时间：2026-07-09
> 方法：**主 Agent（我）全量扫描 + 3 个独立子 Agent 各自全量扫描**，四方视角互相交叉验证。
> 范围：根目录全部文档、`src/` 全部 Python、`src/ui/index.html`、`scripts/`、`tests/`。
> 目的：搞清楚项目是干嘛的、找缺失/bug/可优化点、按 `ACCEPTANCE.md` A–I 级给出验收判断。

---

## 0. 四方是怎么扫的

| 视角 | 侧重 | 是否独立全扫 |
|---|---|---|
| 主 Agent（我） | 文档权威链 + 契约节点 + 红线 grep + 结构 + 关键冲突复核 | ✅ 全扫 |
| 子 Agent A | 后端逻辑 / 商业契约 / 扣次守卫 / 安全 / 文档↔代码↔契约一致性 | ✅ 全扫 |
| 子 Agent B | 前端契约 / 桥差集 / 登录分层 / 状态机 / XSS / B/C/D/G/H 验收 | ✅ 全扫 |
| 子 Agent C | 架构 / 安全红线 / 跨仓契约逐字段 / 测试盲区 / 发布就绪度 | ✅ 全扫 |

每个子 Agent 都被明确要求**独立读完全部文件**，不是分工扫一块（符合你的要求）。

---

## 1. 项目是干嘛的（四方共识）

**胖虎AI客户端**是一款面向买家的 Windows/macOS 桌面 AI 客户端（Python + pywebview 单文件 WebView 前端 `src/ui/index.html` + 后端 `src/panghu_ai_client.py` 及拆分模块）。以单一胖虎AI账号登录为入口，把 **Agent 安装配置与交付验收（Codex / ClaudeCode / OpenClaw / Hermes / 反重力 agy）**、**胖虎AI网站**、**增值业务（AI订阅商城 / 接码 / Plus）**、**代理中心**整合进一个内壳。商业规则（价格/权益/扣次/返佣/代理账本）一律由服务端下发，并经 **Ed25519 商业清单签名**保护，本地只做入口与状态展示。

一句话：**当前处于「文档收束 + 后端审计 + 功能闭环补齐」阶段，未进入发布**（与 `FINAL_REPORT.md` 自述一致）。

---

## 2. 交叉验证矩阵（谁发现、什么级别、最终裁定）

| # | 发现 | 主 | A | B | C | 级别 | 最终裁定 |
|---|---|---|---|---|---|---|---|
| 1 | 前端 5 个必留契约节点齐全（#loginGate/#consoleLayout/updatePythonState/appendPythonLog/renderLogs） | ✅ | — | ✅ | — | 通过 | ✅ 一致 |
| 2 | 登录前不暴露控制台 / 不回退 Tkinter / 不自动开系统浏览器冒充闭环 | ✅ | ✅ | ✅ | ✅ | 通过 | ✅ 一致 |
| 3 | 扣次 fail-closed 守卫正确（未过验收不扣次、异常释放不扣次、Codex 没装不扣次） | — | ✅ | — | — | 通过 | ✅ 一致 |
| 4 | 脱敏 / manifest Ed25519 验签 / 密码 DPAPI / token 不落 profile | — | ✅ | ✅ | ✅ | 通过 | ✅ 一致 |
| 5 | `index.html:1939-1941` 硬编码价格 | —(漏) | — | P2 | **P0** | **冲突** | **降级 P2**：位于 `if(isBrowser)` 预览桩，生产 pywebview 不执行（见 §3 详解） |
| 6 | `commercial_api.py:904` 用 `"price_"+"cents"` 拼接规避打包扫描 | — | — | — | P2 | 冲突 | **保留 P2**：主动绕过审计护栏，确属卫生隐患 |
| 7 | `delivery_scope` 词汇三方漂移（codex_agent_config vs cli/client/both vs full_config…hidden） | — | P1 | — | P1 | 一致 | **P1**：统一词汇表 |
| 8 | 生产 `/api/agent/*`、`/api/communication-software-link/*`、`/api/vas/*` 仍 404 → D/G/H 真实闭环不可验 | — | P1 | P1 | P1 | 一致 | **P1**：硬阻塞 |
| 9 | B 级「切换账号 / 切换主题」设置菜单缺失 | — | — | P1 | — | 一致 | **P1**：ACCEPTANCE B 级明确要求，后端 `set_theme`/`set_buyer_product` 已留未接 |
| 10 | 生产签名公钥须与**生产私钥**配对（否则 manifest 全拒） | — | — | — | P1 | 一致 | **P1**：发布前必查 |
| 11 | 测试非密封（沙箱顺序依赖失败 / 删除拦截） | — | P2 | — | — | 一致 | **P2**：隔离后 374 可复现 |
| 12 | `/api/referrals/bind` 死路径（合同明示服务端无此路由） | — | — | — | P1→ | 一致 | **P2**：潜伏、不炸（HANDOFF §7 已记） |
| 13 | 文档状态漂移（APP_VERSION 1.0.16 vs 文档 1.0.15 / CI v1.0.11；FINAL/RUNBOOK/ACCEPTANCE 仍记 WARN） | — | P2 | — | P2 | 一致 | **P2**：按 AGENTS.md 权威顺序更新 |
| 14 | CSL 表单在后台推送重建画布时丢未暂存输入 | — | — | P2 | — | 一致 | **P2** |
| 15 | 主文件 418KB 单文件 + `from x import *` 全局符号（技术债） | — | — | — | T1/T2 | 一致 | **技术债**：按 AGENTS.md §6 规划下沉 |
| 16 | `scripts/local_integration_server.py:19` 无效转义 `\S` SyntaxWarning | ✅ | — | — | — | 一致 | **P2**：改原始字符串 |
| 17 | `build_config` 写死 `model="gpt-5.5"` | ✅ | — | — | — | 一致 | **已知非 bug**（HANDOFF §4#1 设计取舍） |

> 主 Agent 自己漏掉了第 5 项（grep 模式只匹配 `price = \d` 没匹配 `price_cents:`），靠子 Agent C 的警报触发了主 Agent 的直接读码复核——这正是四方交叉的价值。

---

## 3. 关键冲突详解：前端「硬编码价格」到底是 P0 还是 P2

**子 Agent C 的警报**：`index.html:1939-1941` 硬编码 `price_cents: 9900/29900/19900`，且 `index.html:2123` 注释称「AI订阅商城是客户端内置店铺，不是服务端目录项」→ 判定 **P0 红线违规**（违反「不得硬编码价格」「客户端只展示服务端返回」）。

**主 Agent 直接读码复核结论（裁定）**：
- 这 3 个价格字面量**全部位于 `if (isBrowser) { … }` 预览桩内**（行 1926–2001）。`isBrowser` 仅在「用浏览器直接打开 index.html、没有 pywebview 运行时」时为 true；生产 pywebview 运行时 `isBrowser=false`，**该分支根本不执行**。
- 生产环境的商品与价格来自真实后端 `subscription_list_products`（调用 `/api/subscription/products`，07-09 nginx 路由已修复），服务端下发、前端只渲染 `price_cents`。
- 因此 **不构成生产环境的红线违规** → C 的 P0 判级**过重**，B 的 P2 判级**准确**。

**但 C 的两个底层担忧仍成立，保留为 P2**：
1. 「内置店铺」架构意图（`index.html:2123`）与「服务端是单一真相」原则存在张力：ai_subscription 入口被强制保留，若服务端也下发 AI订阅商品会双源并存。建议明确边界并以服务端目录为价格真相。
2. **`commercial_api.py:904`** 写死 `price_key = "price_" + "cents"  # 避免源码里出现被打包扫描禁止的字面量键`——这是**主动拼接规避审计扫描**。无论意图如何，它削弱了构建期硬编码扫描的可信度，必须改为正常字面量并纳入扫描（P2，安全卫生）。

**处置建议**：把 `isBrowser` 预览桩（含价格字面量）外置为独立测试夹具，使生产 `index.html` 零商业字面量；撤销 `price_`+`cents` 拼接。

---

## 4. 汇总发现清单（按优先级）

### P0 — 红线级
**四方交叉后结论：当前无生产环境 P0 红线违规。** 原 C 提出的硬编码价格经验证为预览桩（非生产路径），降级 P2。

### P1 — 发布前必须处理
1. **生产服务端路由 404（D/G/H 硬阻塞）**：`/api/agent/*`、`/api/communication-software-link/*`、`/api/vas/*` 生产实测 404（`docs/交付状态_2026-07-09.md:85` 自认）。代理中心、连接通讯软件、增值业务集成的**真实闭环在此环境下无法验收**。需补 nginx 精确 location（类比已修的 subscription 三路径）。
2. **`delivery_scope` 词汇三方漂移**：合同 dataclass 默认 `codex_agent_config`（`commercial_backend_contract.py:20/35/51`、`commercial_api.py:461/983`），core 枚举 `DeliveryScope` 为 `full_config…hidden`（`commercial_core.py:17-26`），文档称 `cli/client/both`；且 `parse_delivery_scope` 未知值静默回退 `INSTALL_GUIDED`（`commercial_core.py:1188-1192`）会吞掉服务端真实值。→ 统一词汇表，消除静默回退。
3. **B 级「切换账号 / 切换主题」设置菜单缺失**：`index.html:1800-1808` 顶栏仅有 检查更新/显示诊断/退出登录；后端 `set_theme`(`:5460`)、`set_buyer_product`(`:5452`) 已暴露未接线。`ACCEPTANCE.md` B 级明确要求该菜单。
4. **生产签名密钥配对待确认**：`_build_all.ps1`/CI 注入的公钥须与**生产 Ed25519 私钥**配对，否则生产 manifest 验签全拒、商业配置阻断（`commercial_core.py:1020-1025`）。

### P2 — 优化 / 工程卫生
5. `isBrowser` 预览桩硬编码价格残留在生产文件 → 外置为测试夹具。
6. `commercial_api.py:904` `"price_"+"cents"` 拼接规避扫描 → 改为正常字面量并纳入扫描。
7. 测试非密封（A）：沙箱全量 pytest 因删除拦截 + 共享 `APPDATA` 出现顺序依赖失败（约 25 项）；改 `tmp_path`/隔离 `HOME`/`APPDATA`，CI 注入公钥使「374 passed」基线任何干净环境可复现。
8. `/api/referrals/bind` 死路径（`commercial_api.py:134,539`、`panghu_ai_client.py:1907`、mock `local_integration_server.py:223`）→ 删除或同步合同标注服务端已支持。
9. 文档状态漂移：APP_VERSION `1.0.16`(`panghu_constants.py:10`) vs 文档 1.0.15 / CI `v1.0.11`(`build-mac-release.yml:12`)；`FINAL_REPORT/RUNBOOK/ACCEPTANCE` 仍记 WARN/blocked，未同步 07-09 Windows 包 PASS 状态。按 `AGENTS.md` 权威顺序更新。
10. CSL 表单在后台推送重建画布时可能丢未暂存输入：`captureCanvasFormState/restoreCanvasFormState`(`index.html:3016-3046`) 未覆盖 `csl*`，扩大捕获范围。
11. gemini_agy `GOOGLE_GEMINI_BASE_URL` 复用 `DEFAULT_BASE_URL`（`panghu_agents.py:198`）→ 确认 Gemini 网关确以此基址服务。
12. 前端桥暴露全部公开方法（含 `_` 前缀）（B）→ 建议加白名单收窄。
13. `scripts/local_integration_server.py:19` 无效转义 `\S` SyntaxWarning → 改原始字符串 `r"…"`。
14. 主文件 418KB 单文件 + `from x import *` 全局符号（C T1/T2）→ 按 `AGENTS.md §6` 规划逐步下沉密钥存储/DPAPI/登录存储/格式化助手。

---

## 5. 对照 ACCEPTANCE.md A–I 级验收判断（四方汇总）

| 级 | 主题 | 裁定 | 关键依据 |
|---|---|---|---|
| A | 代码健康 | ✅ 通过（本地） | py_compile 本环境 OK；pytest 374 在开发环境通过，沙箱需隔离复验 |
| B | 客户界面 | ⚠️ 部分通过 | 登录闸口/四模块/退出登录✅；「切换账号/切换主题」菜单缺失（P1-3） |
| C | 网站入口 | ⚠️ 代码级通过 | 入口均指向 aitokenapi.cc；pywebview 缺失时阻断不冒充；需重建 `.venv` 复验 |
| D | 代理中心 | ❌ 真实闭环未验 | 前端契约通过；生产 `/api/agent/*` 404，服务端快照未接 |
| E | Agent 交付 | ⚠️ 逻辑通过 / 未完全闭环 | 扣次 fail-closed 正确；Gemini/客户端 scope 真机未验收 |
| F | 安全与商业边界 | ✅ 通过 | 脱敏/验签/DPAPI 合规；原 P0 硬编码价格经核实非生产违规；`price_`+`cents` 留 P2 |
| G | 连接通讯软件 | ❌ 真实闭环未验 | 独立订单/验收/幂等逻辑通过；生产 `/api/communication-software-link/*` 404 |
| H | 跨项目增值业务 | ❌ 真实闭环未验 | 前端契约通过；生产 `/api/vas/*` 404 |
| I | 发布前 | ❌ 不通过 | D/G/H 真实服务端未通 + 文档/包状态待同步 |

---

## 6. 「已知非 bug」独立复核（子 Agent A 复核，主 Agent 确认）

| 条目（HANDOFF §0.5） | 结论 |
|---|---|
| `copy_logs` WebView 空转、tk 有测试，不能删 | ✅ 非 bug |
| 通用 `/api/payment/orders` 走 page.pay（旧/备用，未被客户端消费） | ✅ 非 bug |
| `alipay.py` 时间戳用服务器本地时区 | ✅ 可接受 |
| `build_config` 写死 `gpt-5.5`（HANDOFF §4#1） | ✅ 设计取舍，非 bug |
| 撤单退款走人工后台 | ✅ 设计取舍 |
| client 形态「装好+配置写入即合格」验收口径 | ✅ 设计取舍 |

---

## 7. 总体结论

**❌ 未通过整体验收，当前不可进入发布。**

代码健康层（A 级）、离线商业合同层、脱敏/manifest 验签/cookie 边界框架设计正确，**四方未发现生产环境 P0 红线违规**。主要阻塞在：
1. D/G/H 级所需的**真实服务端数据链路在生产仍 404**（你侧生产 nginx 路由未补齐）；
2. 几处**契约/文档漂移**（delivery_scope、版本号、状态文档未同步）；
3. B 级**设置菜单缺口**；
4. 预览桩价格字面量 + `price_`+`cents` 扫描绕过等卫生项。

这与 `FINAL_REPORT.md` 自述的「后端审计 + 功能闭环补齐」阶段完全吻合。

---

## 8. 最高优先级 3 条行动项

1. **【P1·真实闭环前提】打通生产服务端路由**：按 `docs/交付状态_2026-07-09.md:43-83` 的零碰撞方案，补 `/api/agent/*`、`/api/communication-software-link/*`、`/api/vas/*` 精确 nginx location，随后用真号真钱做代理中心/连接通讯软件/增值业务端到端验收。在此之前 D/G/H 只能记为「前端契约通过、真实闭环待验」。
2. **【P1·契约/发布安全】统一词汇 + 清理死路径 + 确认密钥配对**：统一 `delivery_scope`（cli/client/both ↔ 内部细分）并消除静默回退；删除 `/api/referrals/bind` 死路径；验证注入公钥与**生产私钥**配对。
3. **【P1/P2·界面与卫生】补齐 B 级设置菜单；外部化预览桩价格字面量并撤销 `price_`+`cents` 拼接；隔离测试使 374 基线可复现；按 `AGENTS.md` 权威顺序把 `FINAL_REPORT/RUNBOOK/ACCEPTANCE` 同步到 07-09 实际状态（Windows 包 PASS、Mac 待验证）**，再依 `RUNBOOK.md` §13 重评 I 级。

---

_四方视角：主 Agent（我）+ 子 Agent A（后端/契约/安全）+ 子 Agent B（前端/验收/UX）+ 子 Agent C（架构/安全/缺失）。交叉验证完成。_
