# 胖虎AI多 Agent 一键部署工具项目蓝图

最后更新：2026-06-26

## 0. 本文件职责

本文件只负责项目蓝图：

- 产品目标
- 范围与非目标
- 固定架构
- 工程边界
- 分工边界
- 验收原则

本文件不负责：

- 运行命令细节
- 当前阻塞日报
- 最终交付状态
- 客户可见产品文案逐条定义

对应文件：

- 客户可见产品口径：`docs/PRODUCT_MANUAL_SINGLE_SOURCE_OF_TRUTH.md`
- 技术维护与接口：`docs/TECHNICAL_MAINTENANCE_MANUAL.md`
- 当前阶段计划：`PLAN.md`
- 节点依赖与状态：`TASK_GRAPH.md`
- 验收清单：`ACCEPTANCE.md`
- 本地运行和验证：`RUNBOOK.md`
- 当前真实状态：`FINAL_REPORT.md`

## 1. 产品目标

本项目是客户侧桌面软件，目标是在客户登录胖虎AI账号后，让客户尽量在同一个工具内完成：

- 查看账号与权益状态。
- 打开胖虎AI网站的注册、邀请码、充值购买、创建 API Key、代理中心等服务端页面，并在代理中心承接本工具独立代理业务入口。
- 完成 Codex、ClaudeCode（CC）、OpenClaw、Hermes 的安装、配置、启动检测、最小中文对话验收和交付矩阵确认；Gemini / agy 当前只保留官方入口和待接入状态，不能计入完整配置交付。
- 作为独立增值服务配置“手机控制Agent”，把已可用 Agent 接入手机通讯或办公通道；该服务独立下单、独立配置、独立验收、独立收费，不回滚基础 Agent 配置交付。
- 生成客服可读的 Execution Trace Log 和最终交付结论。

完成态必须以真实客户闭环为准，不能以单元测试通过、局部截图或假数据演示替代。

## 2. 范围

当前范围内：

- Tkinter 登录闸口和登录后主控制台。
- 顶部一级模块：配置Agent、胖虎AI网站、增值业务、代理中心。
- 内置网站入口与 WebView / 浏览器回退机制。
- 代理中心模块的服务端合同框架：token 返佣、下游付费激活返佣、付费安装 Agent 返佣、下游客户和工具代理后端。
- 已接入 Agent 的安装、配置写入、启动检测、最小对话验收框架；Gemini / agy 保留官方入口和待接入边界。
- 手机控制Agent的产品、后端合同、入口、验收和安全边界；支持已有 Agent 检测或人工复核，不把入口硬绑定到本次基础 Agent 配置完成。
- 商业合同、manifest 验签、权益、配置会话、扣次与交付矩阵代码框架。
- 工程文档、测试、截图和验收流程。

当前范围外：

- 生产服务器、数据库、支付、钱包、下载页、`latest.json`。
- GitHub Release、客户 zip/exe/app 重包与发布。
- 把胖虎AI网站完整重写进 Tkinter。
- 用本机残留配置冒充真实客户交付。

## 3. 固定结构

登录前：

- 只显示登录闸口。
- 只显示最少说明、账号输入、注册入口和公共域名。
- 不显示完整控制台、Agent 矩阵、权益详情或执行日志。

登录后：

- TopBar
- 顶部一级模块导航
- 左侧当前模块子导航
- 中间主工作区
- 右侧账号 / 权益 / 交付状态面板
- 底部 Execution Trace Log

## 4. 固定边界

- 胖虎AI公共域名固定为 `https://aitokenapi.cc`。
- 注册、邀请码、创建 Key、充值购买、代理中心必须走胖虎AI网站或服务端。
- 代理身份只作为登录后账号权益展示。
- 代理中心不能等同于网站推广返佣页；它必须预留 token 返佣、下游付费激活返佣、付费安装 Agent 返佣和工具代理后端合同。
- API Key 不输出到日志。
- 不保存可恢复登录 token 或部署授权 token。
- `profile.json` 只保留账号提示、API Key、模型和界面偏好。
- 未通过功能验收矩阵不得包装成完整交付。
- 手机控制Agent不得作为基础 Agent 配置的子状态；不得共用扣费事件；已形成手机控制验收证据后，客户断网、禁 Key 或取消平台授权不得自动造成免单。

## 5. 工程边界

- 产品定义只从 `docs/PRODUCT_MANUAL_SINGLE_SOURCE_OF_TRUTH.md` 取。
- 技术实现和构建规则只从 `docs/TECHNICAL_MAINTENANCE_MANUAL.md` 取。
- 运行命令只从 `RUNBOOK.md` 取。
- 当前真实进度和阻塞只从 `FINAL_REPORT.md` 取。

不允许把 README、分支交接包、过期截图、聊天上下文当成新的长期规则源。

## 6. 分工边界

前端审计或实现：

- 负责布局、信息层级、视觉稳定性、登录前后分层、截图验收。
- 不负责商业合同、扣次、服务端规则。

后端审计或实现：

- 负责登录、授权、manifest、权益、配置会话、扣次、日志脱敏、`profile.json` 边界。
- 不负责视觉美化。

主线：

- 负责统一文档、集成结果、测试、验收、最终状态报告。
- 不应在上下文已经过长时继续无边界并行大改前后端。

## 7. 完成态原则

节点只有满足以下条件，才可标为已验证：

- 有代码实现或明确外部服务入口。
- 有自动测试、手动操作证据、截图、日志或矩阵记录。
- 没有违反产品手册和技术维护手册。

单元测试通过只代表代码层验证通过，不代表客户可交付。
