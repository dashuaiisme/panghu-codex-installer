# 前端分支交接包

最后更新：2026-06-26

## 角色

你负责“胖虎AI”的 Tkinter 前端视觉、交互结构和截图验收。

## 必读文件

1. `C:\Users\Administrator\.codex\进化.md`
2. `docs\PRODUCT_MANUAL_SINGLE_SOURCE_OF_TRUTH.md`
3. `docs\TECHNICAL_MAINTENANCE_MANUAL.md`
4. `PROJECT_BLUEPRINT.md`
5. `ACCEPTANCE.md`
6. `SAFETY.md`

## 当前目标

让工具更像客户可用的商业桌面软件，而不是脚本工具。重点是视觉稳定、信息结构清晰、客户知道当前做什么、下一步做什么、哪里还没完成。

## 必须保持

- 未登录是登录闸口，不展示完整控制台。
- 登录后固定六块：TopBar、一级模块导航、左侧子导航、中间主工作区、右侧状态面板、底部 Execution Trace Log。
- 顶部一级模块：配置Agent、胖虎AI网站、增值业务、代理中心。
- 配置Agent 一次只显示一个步骤，不把所有步骤堆到一页。
- 配置Agent 子导航需要预留“手机控制Agent”和“手机控制Agent交付验收”。这是独立增值服务入口，不能被本工具本次基础配置完成状态硬锁死；已有可用 Agent 也应能进入检测和配置。
- Agent 交付状态必须保留；Codex、ClaudeCode、OpenClaw、Hermes 展示五维状态，Gemini / agy 未接入前只能显示官方入口或待接入状态。
- 中文界面。
- 浅色企业级商业控制台，苹果风钛金白 / 浅蓝主色。

## 重点优化

- 顶栏品牌区：胖虎头像、产品名、账号、权益、检查更新要对齐稳定。
- 顶部模块导航：减少拥挤，当前模块选中态明确。
- 左侧子导航：只展示当前模块内容，文案短而清楚。
- 中间主工作区：配置Agent是单步骤卡片；胖虎AI网站模块体现内置网站服务；增值业务和代理中心有清晰占位或服务端入口。
- 增值业务可展示“手机控制Agent”销售卡片，但正式配置入口必须回到“配置Agent -> 手机控制Agent”。
- 右侧面板：压缩信息密度，但保留账号、权益、公共域名和 Agent 交付状态。
- 底部日志：轻量、可复制、不要像黑色运维终端。
- 普通窗口和全屏窗口下比例稳定，不出现大量空白或布局拉垮。

## 禁止

- 不改商业合同、扣次、manifest、Agent 安装命令。
- 不碰生产、打包、Release、下载页、`latest.json`。
- 不删除 `release/`、客户包、`docs/superpowers/`。
- 不硬编码价格、次数、有效期、设备数、返佣比例、商品上架状态。
- 不把外部网站购买规则复制到客户端。
- 不把手机控制Agent放到代理中心；不把手机控制Agent做成基础 Agent 交付的同一个验收状态。

## 验收输出

必须输出：

- 改动文件清单。
- 真实截图，至少包含登录页、配置Agent、胖虎AI网站、增值业务、代理中心、普通窗口、全屏窗口。
- 哪些 UI 问题已解决。
- 哪些仍需后端或主线配合。
- 已运行的命令和结果。

## 当前特别提醒

不要说“内置浏览器已经完全闭环”，除非你真实验证了注册、邀请码、充值购买、创建 Key、代理中心全部在工具内完成。
